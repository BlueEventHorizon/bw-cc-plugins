# 追加開発ワークフロー仕様

**目的**: 既存プロジェクトに機能を追加する際の「追加開発」ワークフローの判定基準と手順を定義する。

---

## 1. 適用条件（いつ追加開発として扱うか）

以下をすべて満たすときに追加開発と判定する:

- 既存プロジェクトに **新しい feature** を追加する
- 要件定義書を **新規作成** する（既存要件定義書の追記・更新は対象外）
- 旧仕様（既存のコード・設計書・計画書・要件定義書）が既に存在する

**対象外**:

- `main` の初期立ち上げ（既存仕様が存在しない）
- 既存要件定義書の追記・更新
- バグ修正・リファクタリング

追加開発に該当する場合は `requirement_format.md:7-21` で定義された `type: temporary-feature-requirement` frontmatter を文書先頭に付与する。

---

## 2. 旧仕様との矛盾時の優先度

追加開発の要件定義書は **旧仕様より優先する正本** として扱う:

| 比較対象             | 優先度           |
| -------------------- | ---------------- |
| 追加開発の要件定義書 | **最優先（正）** |
| 既存の要件定義書     | 旧               |
| 既存の設計書         | 旧               |
| 既存の計画書         | 旧               |
| 既存のソースコード   | 旧               |

判断例:

- 新要件「ユーザーはゲスト状態でも閲覧可能」 vs 旧要件「全機能は認証必須」 → 新要件を採用
- 新要件で既存 API の仕様が変わる → 新要件に従って実装する（旧設計書との不整合は merge 時に解消する）

---

## 3. 新規ファイル分離の判断基準

追加開発の実装期間中、**旧仕様ファイルは書き換えない**。以下の判断基準に従う:

| ケース                               | 判断                                                     |
| ------------------------------------ | -------------------------------------------------------- |
| 新機能のコード                       | 新規ファイル・新規ディレクトリに作成                     |
| 既存コードに分岐を追加する必要がある | 既存コードを最小限のタッチで拡張（新条件の追加のみ）     |
| 既存関数のシグネチャを変える         | 新関数を追加し、旧関数は残す（merge 時に旧関数を削除）   |
| 既存設計書・計画書に矛盾             | 追加開発の要件定義書を正として扱い、既存文書は変更しない |

**理由**: 旧仕様ファイルを変更すると、追加開発が完了する前に旧仕様が壊れる。merge タイミングで一括反映することで、仕様と実装の一貫性を保つ。

---

## 4. merge タイミングと手順

追加開発の完了後、以下の手順で一時文書を旧仕様へ統合する:

1. **前提条件の確認**
   - 実装が完了している（計画書の全タスクが completed）
   - `/forge:review` によるレビューと修正が完了している
   - テストが通っている
2. **merge 対象の特定**
   - 追加開発の要件 → 既存の要件定義書へ反映
   - 追加開発の設計 → 既存の設計書へ反映
   - 追加開発の計画 → 破棄（既に実装済み）
3. **merge 実行**
   - 既存文書に新要件・新設計を統合し、整合を取る
   - 新規追加したコード・関数を恒久的な配置に移動（必要なら）
4. **一時文書の削除**
   - `type: temporary-feature-requirement` の要件定義書を削除
   - 対応する設計書・計画書も削除
5. **ToC 再生成**
   - `/doc-advisor:create-specs-toc` で specs ToC を更新

merge が完了するまで、一時文書は残す。

---

## 5. 関連文書

- [`requirement_format.md`](requirement_format.md) — `type: temporary-feature-requirement` frontmatter の正式定義
- [`spec_design_boundary_spec.md`](spec_design_boundary_spec.md) — What/How 境界の判断（merge 時の振り分けに活用）
- [`design_principles_spec.md`](design_principles_spec.md) — 設計原則（新規ファイル分離の判断補助）
