{{PROTOCOL_HEADER}}

## レビュー依頼（コード）

レビューのみを行い、対象ファイルを変更しないでください。修正の実施主体は依頼元です。

## レビュー対象

プロジェクトルート: `{{PROJECT_ROOT}}`

対象:

{{TARGET_SCOPE}}

上記の対象を全文読んでレビューしてください。

**これは差分レビューではありません。** 未コミット変更の有無・git 履歴は対象の決定に関係しません。変更されていないファイルも等しくレビュー対象です。

ディレクトリが示されている場合は、その配下のファイルを**あなた自身が列挙し、各ファイルを全文読んでください**。

## 今回の到達目標と意図的な未実装

{{SCOPE}}

この欄が `（指定なし）` の場合、対象は最終形であるとみなしてレビューしてください。「情報が無い」ではなく「最終形である」の意味です。

宣言された未実装が、設計書・仕様書の記述と矛盾を生んでいる場合は、その事実を所見として報告してください。**「後続タスクで実装予定」であることは、現時点で設計と実装が乖離している事実を免除しません。** 到達目標はレビューの評価基準を伝えるものであり、範囲外の指摘を封じるものではありません。

## 今回の重点観点

{{FOCUS}}

これは下記の参照文書に基づく通常のレビューに**加えて**特に注意を払う対象であり、他の観点を免除するものではありません。`（指定なし）` の場合は通常のレビューのみを行ってください。

## 参照文書

### レビュー観点

- `{{PLUGIN_ROOT}}/docs/criteria/review_criteria_code.md`

### 優先度体系と重大度の判断基準

- `{{PLUGIN_ROOT}}/docs/review_priorities_spec.md`

### 規範（違反を検出する対象）

- `{{PLUGIN_ROOT}}/docs/forge_anti_patterns.md` — 言語・プロジェクトに依らない汎用アンチパターン

### プロジェクト固有のルール文書

{{PROJECT_RULES}}

### 関連するプロジェクト仕様書

{{PROJECT_SPECS}}

> **コードレビューの規範はプロジェクト側にあります。** forge が持つのは上記の汎用アンチパターンのみで、コーディング規約・アーキテクチャ規約・命名規則・レイヤ境界などの実質的な規範は「プロジェクト固有のルール文書」に列挙されたものだけです。
>
> **この欄が空、または対象コードに対応する規約が見当たらない場合は、その事実を所見として報告してください。** 規範なしでレビューしたことが分からないまま承認されるのを避けるためです。その場合は「どのような規約が必要か」の草案を添えてください（依頼元がルール追加の Issue を起票します）。

## 返信形式契約

所見は自由記述 markdown で記述し、各所見に重大度マーカー（🔴 critical / 🟡 major / 🟢 minor）と `ファイルパス:行` の位置情報を付与してください。

返信の最終行には、次のいずれかの完了宣言行を必ず 1 行だけ置いてください。

- `REVIEW_RESULT: approved`（指摘なし・承認）
- `REVIEW_RESULT: findings`（指摘あり）

この完了宣言行は受信側の完了判定で機械的に照合される唯一の契約です。それ以外の本文は自由記述で構いません。

改めて明記します: 対象ファイルを変更しないでください。ファイルの変更は禁止です。
