{{PROTOCOL_HEADER}}

## レビュー依頼（機密情報の混入）

レビューのみを行い、対象ファイルを変更しないでください。修正の実施主体は依頼元です。

## レビュー対象

プロジェクトルート: `{{PROJECT_ROOT}}`

対象は **リポジトリ全体**（git 管理下のファイル、および `.gitignore` されていない未追跡ファイル）です。差分ではありません。

差分に限定しない理由: 過去のコミットで混入した秘密は今回の差分に現れませんが、リポジトリには残っており漏洩の対象です。差分に絞った時点でこのレビューの目的を達成できません。

## 機械検出の結果

以下は決定論的スキャン（`scan_secrets.py`）の検出結果です。**値は復元できない形にマスクしてあります**。実体を確認するには、示された位置のファイルを開いてください。

### 検出（要判定）

{{SCAN_FINDINGS}}

### 抑制マーカーで分類された検出

{{SCAN_SUPPRESSED}}

行末に `secrets-scan: ignore` があるため上と分けていますが、**除外済みという意味ではありません**。マーカーが付いている事実は混入でないことの根拠になりません。同様に確認してください。

### 走査の統計

{{SCAN_STATS}}

`filtered` はスキャンが機械的に「秘密ではない」と判定して除外した件数です（プレースホルダ・コード式・パスに見えるキー）。除外規則が強すぎる兆候がないかの参考として示します。件数が不自然に多い場合はその旨を所見に含めてください。

## あなたに判断してほしいこと

**1. 各検出の真偽判定**

検出結果をそのまま所見にしないでください。スキャンは形式が一致しただけで、文脈を見ていません。該当箇所を開き、真の混入か、許容される書き方（プレースホルダ・環境変数参照・公開前提の値）かを判定してください。

**2. スキャンが拾えない混入の捜索**

スキャンは既知の形式にしか一致しません。この工程を省くと、スキャンのパターン網羅性がそのままレビューの上限になります。とくに次を探してください。

- 形式を持たないパスワード・内部エンドポイント・プライベート IP
- 設定手順やトラブルシュート記述の散文に埋め込まれた資格情報
- 実在の値をコピーして数文字だけ改変した「それらしい例示」
- 実在する個人を特定できる情報、顧客固有の識別子、実運用のリソース名

## 今回の重点観点

{{FOCUS}}

これは上記の判断に**加えて**特に注意を払う対象であり、他の観点を免除するものではありません。`（指定なし）` の場合は通常のレビューのみを行ってください。

## 参照文書

### レビュー観点

- `{{PLUGIN_ROOT}}/docs/criteria/review_criteria_secrets.md`

### 優先度体系と重大度の判断基準

- `{{PLUGIN_ROOT}}/docs/review_priorities_spec.md`

### 規範（違反を検出する対象）

- `{{PLUGIN_ROOT}}/docs/sensitive_information_spec.md` — 含めてはならないもの・許容される書き方・重大度カタログ

### プロジェクト固有のルール文書

{{PROJECT_RULES}}

### 関連するプロジェクト仕様書

{{PROJECT_SPECS}}

> **プロジェクト固有の公開方針があればそちらが優先します。** 上欄が空の場合、内部ホスト名・顧客識別子・固有名の扱いは forge 内蔵の規範のみで判定することになります。判定に迷う項目があれば、その旨を所見に含めてください（依頼元がルール追加の Issue を起票します）。

## 所見の書き方 [重要]

**検出した秘密の実体を返信本文に書かないでください。** ファイルパスと行番号、種別、判断根拠のみを書いてください。

このやり取りはメッセージ DB に永続化されます。実値を返信に含めると、混入を指摘する行為そのものが新たな複製経路になります。

## 返信形式契約

所見は自由記述 markdown で記述し、各所見に重大度マーカー（🔴 critical / 🟡 major / 🟢 minor）と `ファイルパス:行` の位置情報を付与してください。

返信の最終行には、次のいずれかの完了宣言行を必ず 1 行だけ置いてください。

- `REVIEW_RESULT: approved`（指摘なし・承認）
- `REVIEW_RESULT: findings`（指摘あり）

この完了宣言行は受信側の完了判定で機械的に照合される唯一の契約です。それ以外の本文は自由記述で構いません。

改めて明記します: 対象ファイルを変更しないでください。ファイルの変更は禁止です。
