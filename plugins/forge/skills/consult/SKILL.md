---
name: consult
description: |
  利用者と議論・相談・方針検討を進める対話スキル。論点を立てて検証し、討議ファイルへ記録しながら、
  背景と本質を述べて 1 件ずつ進行する。次に何を扱うかは AI が決める。
  議論モード（既定）は説明までで止まり決定を迫らない。決定モードは論点ごとに推奨を述べて決着させる。
  複数の事柄について人間の理解と判断が要る場面、たたき台のレビュー、調査結果の検討、設計方針の相談、
  仕様の詰めに使う。会話の途中からでも起動でき、既に始まっている議論を引き取れる。
  レビュー所見の仕分けは対象外（`/forge:review` が自前で行う）。
  トリガー: "相談したい", "議論したい", "壁打ち", "たたき台をレビューして", "方針を決めたい",
  "論点を整理して", "どう思う", "ちゃんと説明して", "背景から説明して", "consult"
user-invocable: true
argument-hint: "[議論したい対象や問い]"
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion
---

# /forge:consult

このスキルは議論の進行と記録のみを行う。**親が依頼している他の作業を引き継いではならない。**
会話の途中から起動された場合、引き取るのは**議論の対象**だけであり、進行中の実装・修正・調査を
再開してはならない（議論を引き取ることと、親の作業を続けることは別である）。

利用者との議論を**進行する**スキル。論点を立て、背景と本質を述べ、質疑を経て、決着した内容を
討議ファイルへ記録する。判断に基づく実行（修正・起票・commit）は行わない。

## 規範の読み込み [MANDATORY]

着手前に `${CLAUDE_PLUGIN_ROOT}/docs/consult_principles_spec.md` を Read する。**提示の作法はすべて
同文書が定める。本 SKILL.md に規範を書き写さない**（定義点が 2 つになるため）。本 SKILL.md が定めるのは
手順だけである。

---

## Phase 0: モードの確定

利用者の依頼が**議論**なのか**決定**なのかを先に確定し、確定した内容を 1 行で述べてから進む。

| 依頼の形                                                       | モード | 振る舞い                                         |
| -------------------------------------------------------------- | ------ | ------------------------------------------------ |
| 「たたき台をレビューして」「壁打ち」「議論しよう」「どう思う」 | 議論   | 説明までで止まる。判断を求める問いを一切発しない |
| 「決めたい」「どうすべきか」「方針を確定したい」               | 決定   | 論点ごとに推奨を述べ、応答を受けて決着させる     |

判別できない場合は**議論**を既定とする。議論を決定へ変えるのは利用者の明示指示があったときだけで、
AI から促さない。

## Phase 1: 論点の抽出と検証

1. 対象を把握する（渡された文書・コード・会話の流れ）
2. 論点を立てる。各論点に**識別子**を与える（`01` `02` …。派生は `03a` `03b`）
3. **識別子を後から振り直さない**。利用者が論点を指す手段になるため、決着・取り下げの後も番号を空けたまま維持する
4. **確信度が低く、検証できる論点は、提示する前に検証する**（実物を Read する・コマンドで確かめる・
   履歴を追う）。推測のまま提示して利用者に検証を促されることがないようにする

論点が 0 件なら、討議ファイルを作らずその旨を述べて終える。

## Phase 2: 討議ファイルの用意

### 2.1 既存ファイルの確認

新規に作る前に、同じ主題の討議が中断されていないかを見る。

```bash
mkdir -p .claude/.temp/consult && ls -1 .claude/.temp/consult
```

**状態が「進行中」で主題が一致するファイルがあれば、再開の候補として扱う。** その要点（主題・未判断の
件数・最後に決着した項目）をコンソールに 1〜2 行で示し、**再開するか新規に始めるかを AskUserQuestion で
確認する**（進行上の機械的な二者択一であり、議論の内容ではない）。
状態が「終了」のファイルは候補にしない（新規を既定とする）。

再開する場合、そのファイルのアジェンダと決着を Read し、**既存の識別子をそのまま引き継ぐ**。
番号を振り直さない。決着済みの項目を再び提示しない。

### 2.2 新規作成

`.claude/.temp/consult/` 配下に Markdown で作る。ファイル名は `<日付>-<主題のスラッグ>.md`
（例: `20260811-consult-skill.md`）。同名があれば連番を足す。

書式は `${CLAUDE_SKILL_DIR}/assets/discussion_file_template.md` を Read して従う。**作成した実パスを
コンソールに 1 行で示す**（利用者がエディタで開くため）。

## Phase 3: アジェンダの提示

個別の話に入る前に、アジェンダ表をコンソールへ出す。表は討議ファイルにも同じものを持つ。

全体で何件あり、どれが重いのかを先に見せる。ここで背景を書かない。

## Phase 4: 進行

**重要度の高い順に、AI が自分で 1 件ずつ入る。**

各項目で行うこと:

1. 背景と本質、推奨（決定モードのみ）をコンソールへ述べる。**その項目単体で判断できる要点を必ず書く**。
   討議ファイルにあることを理由に省かない
2. **判断を左右する主張に確信度を付ける**（記号と運用は規範文書の原則 4 に従う）
3. **原文への到達手段を併記する**。要約して提示してよいが、利用者が自分で原文を確かめられるよう
   `ファイルパス:行` や出典を添える。これを欠くと、利用者は要約の正しさを検証できない
4. 利用者の応答を受ける。理解を求める応答（「詳しく」「なぜ」）には、判断を促さず答える
5. 決着したら討議ファイルを更新する（結論・理由・決定の帰属）
6. アジェンダ表を再掲する
7. **次の項目へ自分で進む**

**「次はどれを見ますか」「これで進めてよいですか」と聞いてはならない [MANDATORY]。** 利用者がすることは
止める・飛ばす・順序を変えることだけでよい状態を保つ。

同種の判断が繰り返される項目は、一括の判定表にまとめて提示してよい。往復が判断の質を上げない場面では、
往復を増やすことのほうが害になる。何件からまとめるかは **AskUserQuestion で確認**し、AI が独自に閾値を
定めない（件数の設定であり、議論の内容ではない）。

### 議論の途中で論点が動いたとき

議論の結果、論点が誤りだと判明することがある。その場合:

- **取り下げた論点を削除しない**。状態を「取り下げ」にし、なぜ取り下げたかを結果欄に書く
- 新しく生まれた論点は**新しい識別子**を与えて追加する。既存の番号を再利用しない
- AI 自身の誤認による取り下げは、その旨を明示する（利用者の指摘によるものか、自己訂正か）

## Phase 5: 終了

利用者が終える意思を示したら、その時点で確定する。

- **未判断のまま残った件数を明示する**。未判断を「対応不要」へ畳み込まない
- 討議ファイルの実パスを再度示す
- 決着した内容が恒久的な記録に値する場合、ADR への組み直しを**提案する**（実施はしない。BL-001）

**本スキルは討議ファイルを自分から消さない。** ただし置き場は一時領域であり、作業ツリーの破棄や
クリーンで失われうる（git 管理外）。**恒久的に残すべき内容は ADR 等へ組み直す必要がある**——
上記の提案はそのためにある。寿命そのものは規定しない。

---

## 同梱ファイル

- `${CLAUDE_SKILL_DIR}/assets/discussion_file_template.md` — 討議ファイルの構造と書くときの約束。
  Phase 2 で読む

---

## 禁止事項 [MANDATORY]

| 禁止                                                 | 理由                                                 |
| ---------------------------------------------------- | ---------------------------------------------------- |
| 判断に基づく実行（修正・起票・commit・外部送信）     | 議論の途中で、利用者が決めていない変更が成果物へ入る |
| 実行を担う別スキル・別 Agent の起動                  | 同上                                                 |
| 「次はどれを見ますか」と進行を利用者へ返す           | 進行は本スキルが持つ                                 |
| 討議ファイルにあることを理由にコンソールの要点を省く | ファイルは詳細の置き場であって、提示の省略先ではない |
| 議論モードで判断を促す                               | 議論は決定フローではない                             |
| 機密情報の値を提示・記録する                         | 「どこに」「どの種類か」までに留める                 |

## AskUserQuestion の使い分け [MANDATORY]

境界は「議論の内容か、進行上の機械的な確認か」である。

| 場面                                                                                | 手段                                   |
| ----------------------------------------------------------------------------------- | -------------------------------------- |
| 論点の提示・背景と本質・推奨・議論の選択肢                                          | **地の文**。AskUserQuestion を使わない |
| 進行上の機械的な二者択一（既存討議ファイルの再開 / 新規、一括提示へ切り替える件数） | **AskUserQuestion**                    |

地の文にする理由は、選択肢 UI が背景と本質を置く場所を持たず、規範が求める提示順序
（背景 → 本質 → 推奨 → 選択肢）を満たせないためである。議論の中身を選択肢 UI に載せると、
背景抜きで選ばせる提示になる。

**迷ったら地の文にする。** 「機械的だから」を理由に議論の中身を選択肢 UI へ移してはならない。

**自己呼び出しを行わない [MANDATORY]**。`Skill` ツールで本スキル自身を呼ばない。手順の一部として
「本スキルを起動し直す」と書かない。本スキルは多くの議論の起点に置かれるため、自己呼び出しは
暴走ループになりうる。
