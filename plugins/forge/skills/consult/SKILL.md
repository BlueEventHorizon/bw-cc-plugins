---
name: consult
description: |
  利用者と議論・相談・方針検討を進める対話スキル。論点を立てて検証し、記録・表示機構（agenda）へ
  記録しながら、背景と本質を述べて 1 件ずつ進行する。次に何を扱うかは AI が決める。
  議論モード（既定）は説明までで止まり決定を迫らない。決定モードは論点ごとに推奨を述べて決着させる。
  複数の事柄について人間の理解と判断が要る場面、たたき台のレビュー、調査結果の検討、設計方針の相談、
  仕様の詰めに使う。会話の途中からでも起動でき、既に始まっている議論を引き取れる。
  レビュー所見の仕分けは対象外（`/forge:review` が自前で行う）。
  トリガー: "相談したい", "議論したい", "壁打ち", "たたき台をレビューして", "方針を決めたい",
  "論点を整理して", "どう思う", "ちゃんと説明して", "背景から説明して", "consult"
user-invocable: true
argument-hint: "[議論したい対象や問い]"
allowed-tools: Read, Write, Glob, Grep, Bash, AskUserQuestion
---

# /forge:consult

このスキルは議論の進行と記録のみを行う。**親が依頼している他の作業を引き継いではならない。**
会話の途中から起動された場合、引き取るのは**議論の対象**だけであり、進行中の実装・修正・調査を
再開してはならない（議論を引き取ることと、親の作業を続けることは別である）。

利用者との議論を**進行する**スキル。論点を立て、背景と本質を述べ、質疑を経て、決着した内容を
記録・表示機構（agenda）へ記録する。判断に基づく実行（修正・起票・commit）は行わない。

## 規範の読み込み [MANDATORY]

着手前に `${CLAUDE_PLUGIN_ROOT}/docs/consult_principles_spec.md` を Read する。**提示の作法はすべて
同文書が定める。本 SKILL.md に規範を書き写さない**（定義点が 2 つになるため）。本 SKILL.md が定めるのは
手順だけである。

**本スキルは討議の状態を自ら保持しない**。論点・背景・本質・決着はすべて記録・表示機構（agenda。
`${CLAUDE_PLUGIN_ROOT}/scripts/agenda/agenda_store.py`）へ渡し、そこに一元化する。本 SKILL.md 固有の
状態ファイルは持たない。

---

## Phase 0: モードの確定

利用者の依頼が**議論**なのか**決定**なのかを先に確定し、確定した内容を 1 行で述べてから進む。

| 依頼の形                                                       | モード | 振る舞い                                         |
| -------------------------------------------------------------- | ------ | ------------------------------------------------ |
| 「たたき台をレビューして」「壁打ち」「議論しよう」「どう思う」 | 議論   | 説明までで止まる。判断を求める問いを一切発しない |
| 「決めたい」「どうすべきか」「方針を確定したい」               | 決定   | 論点ごとに推奨を述べ、応答を受けて決着させる     |

判別できない場合は**議論**を既定とする。議論を決定へ変えるのは利用者の明示指示があったときだけで、
AI から促さない。

### 起点の判定 [MANDATORY]

本スキルは、利用者からの直接依頼（**consult 起点**）としても、review 本体からの委譲（**review 起点**）
としても起動する。両者は別プロセス・別セッションではなく、同一の会話が続いたまま振る舞いが切り替わる
だけである。したがって起点は外部から渡される構造化フラグではなく、**AI 自身が置かれている文脈**で
判断する——直前に reviewer/evaluator との往復を行い、結合済みの所見配列（何が問題か・どこを指すか・
重大度・採否の確信度）を会話コンテキストに既に持っている場合は review 起点、それ以外（一般的な議論・
課題解決・たたき台のレビューを問わない）は consult 起点として扱う。

review 起点は、所見への採否を得ることが目的であるため**決定モード**として扱う（上表の判定を経由しない）。

review 起点では **Phase 1 を経由しない**（次節参照）。所見群についての構造的な判断（同型の指摘の
繰り返し・複数指摘が同一の土台に由来していないか）も、review 本体の実行中に既に行われているため、
その内容を Phase 2.2 でそのまま `structural_judgment.note` として渡す。

## Phase 1: 論点の抽出と検証（consult 起点のみ）

**review 起点はこの Phase を飛ばし、Phase 2 から続ける**（起点の判定を参照）。所見配列は
reviewer/evaluator が既に評価済みであり、consult 自身が新たに論点を立てる工程は不要である。

1. 対象を把握する（渡された文書・コード・会話の流れ）
2. 論点を立てる。各論点に**識別子**を与える（`01` `02` …。派生は `03a` `03b`）
3. **識別子を後から振り直さない**。利用者が論点を指す手段になるため、決着・取り下げの後も番号を空けたまま維持する
4. **確信度が低く、検証できる論点は、提示する前に検証する**（実物を Read する・コマンドで確かめる・
   履歴を追う）。推測のまま提示して利用者に検証を促されることがないようにする
5. 論点全体を通した**構造的な判断**（同型の指摘が無いか、個別の食い違いに留まるか等）を 1〜2 行でまとめる。
   Phase 2.2 でこの内容を `structural_judgment.note` として渡す

論点が 0 件なら、記録を作らずその旨を述べて終える。

## Phase 2: アジェンダの用意

### 2.1 既存記録の確認

新規に始める前に、中断中の記録がないかを見る。**確認方法は起点で異なる**（記録の置き場が起点ごとに
固定パスかセッション単位かで変わるため。2.2 参照）。

**review 起点**: review は同時に 1 つのレビューしか実行しないため、記録は固定パスに常に高々 1 つしか
存在しない。列挙は不要で、ファイルの存在を確認してから直接確認する。

```bash
test -f ".claude/.temp/review/agenda.json" && \
  python3 "${CLAUDE_PLUGIN_ROOT}/scripts/agenda/agenda_store.py" pending --path ".claude/.temp/review/agenda.json"
```

ファイルが存在しなければ新規に始める。存在し `remaining_count` が 0 より大きければ、未判断の件数を
1〜2 行で示し、**再開するか新規に始めるかを AskUserQuestion で確認する**。**ファイルが存在するのに
`status` が `error`（JSON 破損等）の場合は「存在しない」と同一視せず、利用者へ報告して対応を確認する**
（`start` は既存内容を無条件に上書きするため、破損記録を新規開始として黙って上書き・消失させない）。

**consult 起点**: 複数の記録が同時に存在しうるため、列挙してから個別に残件を確認する。

```bash
ls -1d .claude/.temp/consult/*/ 2>/dev/null
```

見つかった各ディレクトリについて `agenda.json` の有無を確認し、**存在するものだけ**残件を取得する。

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/agenda/agenda_store.py" pending --path "<ディレクトリ>agenda.json"
```

**残件（`remaining_count`）が 0 より大きい記録があれば、再開の候補として扱う。** その要点（主題＝
`config.identity`・未判断の件数・項目の一覧）をコンソールに 1〜2 行で示し、**再開するか新規に始めるかを
AskUserQuestion で確認する**（進行上の機械的な二者択一であり、議論の内容ではない）。候補が無ければ
新規に始める。**`agenda.json` が存在するのに `status` が `error`（JSON 破損等）の記録は候補から除外せず、
利用者へ報告して対応を確認する**（他の候補と同列に「無かったこと」にしない）。

**いずれの起点でも**、再開する場合はその `agenda.json` の実パスを以降の全コマンドの `--path` に使い続ける。
**既存の識別子をそのまま引き継ぐ**（`agenda_store.py` は識別子を振り直さない）。`decision` が記録済みの
項目は再び提示しない。

### 2.2 新規開始

記録の置き場・`config` は起点で異なる（`config.identity` はいずれもパスの親ディレクトリ名から
`agenda_store.py` 自身が導出するため、AI は渡さない）。

| 起点         | 置き場                                                   | `config.item_fields` | `config.severity_field` |
| ------------ | -------------------------------------------------------- | -------------------- | ----------------------- |
| review 起点  | `.claude/.temp/review/agenda.json`（固定）               | `["severity"]`       | `"severity"`            |
| consult 起点 | `.claude/.temp/consult/${CLAUDE_SESSION_ID}/agenda.json` | `[]`                 | `null`                  |

**review 起点**の候補 JSON:

```json
{
  "structural_judgment": { "note": "<review 本体で判断済みの構造的判断>" },
  "config": { "item_fields": ["severity"], "severity_field": "severity" },
  "items": [
    {
      "id": "<結合済み配列の通し番号>",
      "title": "<所見の要約>",
      "fields": { "severity": "<evaluator が確認・訂正した重大度>" }
    }
  ]
}
```

`items[]` は、reviewer 所見と evaluator 判定を結合した配列（会話コンテキストに既にある）をそのまま用いる。
consult 自身は新たに論点を立てない（Phase 1 を経由しないため）。

**consult 起点**の候補 JSON:

```json
{
  "structural_judgment": { "note": "<Phase 1 手順 5 でまとめた構造的判断>" },
  "config": { "item_fields": [], "severity_field": null },
  "items": [
    { "id": "01", "title": "<短い名前>" }
  ]
}
```

以下、起点を問わず共通の手順である（`<置き場>` は上表の値を使う）。

1. **Write** ツールで候補 JSON を `.claude/.temp/agenda-start-${CLAUDE_SESSION_ID}.candidate.json` へ書く。
   **自由記述文（背景・本質・タイトル等）をシェルコマンドの引用符へ直接埋め込まない**——改行・引用符・
   記号を含む自由記述をシェル文字列に組み立てると、エスケープの崩れによる構文破壊や注入のリスクを生むため、
   常にファイル経由（`--input-file`）で渡す
2. 次のコマンドを **1 回**実行する:

   ```bash
   python3 "${CLAUDE_PLUGIN_ROOT}/scripts/agenda/agenda_store.py" start \
     --path "<置き場>" \
     --input-file ".claude/.temp/agenda-start-${CLAUDE_SESSION_ID}.candidate.json"
   ```

   `status` が `error` の場合、`message` に従って候補 JSON を訂正し、Write から同じ手順をもう 1 回だけ
   実行する。2 回目も失敗した場合は論点の把握に失敗したものとして利用者へ報告する。

3. **開始直後、Bash で表示物を開く**（初回のみ）:

   ```bash
   open "<置き場と同じディレクトリ>/agenda.html"
   ```

   **2 回目以降は `open` を呼ばない**（重複タブを避けるため）。**自動追従の仕組みは持たない**——以降の
   更新を見るには利用者がタブを手動で再読み込みする。

## Phase 3: アジェンダの提示

個別の話に入る前に、アジェンダ表をコンソールへ出す。

`agenda.json`（Read ツール、または `agenda_store.py pending --path <path>` の結果）から、項目の一覧・
残件を読み取り、`consult_principles_spec.md` の表の列に沿ってコンソールへ表として示す。全体で何件あり、
どれが重いのかを先に見せる。ここで背景を書かない。

## Phase 4: 進行

**重要度の高い順に、AI が自分で 1 件ずつ入る。** 次に扱う項目は次のコマンドで得る:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/agenda/agenda_store.py" next --path "<agenda.jsonのパス>"
```

各項目で行うこと:

1. 背景と本質、推奨（決定モードのみ）をコンソールへ述べる。**その項目単体で判断できる要点を必ず書く**。
   記録・表示機構が生成する提示（agenda.html）にあることを理由に省かない
2. **判断を左右する主張に確信度を付ける**（記号と運用は規範文書の原則 4 に従う）
3. **原文への到達手段を併記する**。要約して提示してよいが、利用者が自分で原文を確かめられるよう
   `ファイルパス:行` や出典を添える。これを欠くと、利用者は要約の正しさを検証できない
4. 利用者の応答を受ける。理解を求める応答（「詳しく」「なぜ」）には、判断を促さず答える
5. **背景・本質が分かった時点で record を呼ぶ**（決着を待たない。分かった時点で都度記録する）:

   候補 JSON（例）:

   ```json
   { "background": "...", "essence": "..." }
   ```

   Write ツールで `.claude/.temp/agenda-record-<item-id>-${CLAUDE_SESSION_ID}.candidate.json` へ書き、
   次を実行する:

   ```bash
   python3 "${CLAUDE_PLUGIN_ROOT}/scripts/agenda/agenda_store.py" record \
     --path "<agenda.jsonのパス>" --item-id "<item-id>" \
     --input-file ".claude/.temp/agenda-record-<item-id>-${CLAUDE_SESSION_ID}.candidate.json"
   ```

6. **決着したら record をもう一度呼ぶ**（2 回目の呼び出し）:

   候補 JSON（例）:

   ```json
   { "decision": { "by": "human", "outcome": "adopt", "reason": "..." } }
   ```

   `decision.by` は判断の主体（`"human"` または `"AI"`。AI が代行判断した場合はその旨が判別できる値を書く）。
   `status` が `error` の場合、`missing_fields` に列挙されたフィールドを補って同じ手順をもう 1 回だけ
   実行する。

7. アジェンダ表を再掲する（Phase 3 と同じやり方）
8. **次の項目へ自分で進む**（`next` の返り値を使う）

**「次はどれを見ますか」「これで進めてよいですか」と聞いてはならない [MANDATORY]。** 利用者がすることは
止める・飛ばす・順序を変えることだけでよい状態を保つ。

同種の判断が繰り返される項目は、一括の判定表にまとめて提示してよい。往復が判断の質を上げない場面では、
往復を増やすことのほうが害になる。何件からまとめるかは **AskUserQuestion で確認**し、AI が独自に閾値を
定めない（件数の設定であり、議論の内容ではない）。

### 議論の途中で論点が動いたとき

議論の結果、論点が誤りだと判明することがある。取り下げは**特別なコマンドではない**。通常の `record`
呼び出しで `decision.outcome` に自由文（例: `"取り下げ"`）を書くだけでよい:

```json
{ "decision": { "by": "AI", "outcome": "取り下げ", "reason": "..." } }
```

- **取り下げた論点を削除しない**。上記の record 呼び出しで決着させれば、記録から消えることなく
  残る（agenda 側に削除操作は無い）
- `decision.reason` に、なぜ取り下げたか・誰の指摘によるものか（利用者の指摘か AI の自己訂正か）を書く
- 新しく生まれた論点は**新しい識別子**を与え、新規項目として `record` を呼ぶ。既存の番号を再利用しない。
  **新規追加時は候補 JSON に `title` と `structural_judgment.note`（追加後もなお構造的な誤りが無いことの
  再判断）を同時に含めること**。伴わない場合、`agenda_store.py` は項目・判定ともに保存せず拒否する

## Phase 5: 終了

利用者が終える意思を示したら、その時点で確定する。

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/agenda/agenda_store.py" finish --path "<agenda.jsonのパス>"
```

- `deleted: true` の場合、全項目が決着済みであり記録は削除された旨を伝える
- `deleted: false` の場合、`remaining_count` を**未判断のまま残った件数**としてそのまま提示する
  （未判断を「対応不要」へ畳み込まない）。記録は削除されず、`agenda.json`/`agenda.html` が残る旨も伝える
- 表示物の実パス（`agenda.html`）を再度示す
- 決着した内容が恒久的な記録に値する場合、ADR への組み直しを**提案する**（実施はしない。BL-001）

**本スキルは記録を自分から消さない**（`finish` が全項目決着を条件に消すのみ）。ただし置き場は一時領域
であり、作業ツリーの破棄やクリーンで失われうる（git 管理外）。**恒久的に残すべき内容は ADR 等へ
組み直す必要がある**——上記の提案はそのためにある。

---

## 禁止事項 [MANDATORY]

| 禁止                                                                 | 理由                                                       |
| -------------------------------------------------------------------- | ---------------------------------------------------------- |
| 判断に基づく実行（修正・起票・commit・外部送信）                     | 議論の途中で、利用者が決めていない変更が成果物へ入る       |
| 実行を担う別スキル・別 Agent の起動                                  | 同上                                                       |
| 「次はどれを見ますか」と進行を利用者へ返す                           | 進行は本スキルが持つ                                       |
| 記録・表示機構が生成する提示にあることを理由にコンソールの要点を省く | 提示は詳細の置き場であって、コンソール提示の省略先ではない |
| 議論モードで判断を促す                                               | 議論は決定フローではない                                   |
| 機密情報の値を提示・記録する                                         | 「どこに」「どの種類か」までに留める                       |
| 自由記述文をシェル文字列へ直接埋め込む                               | エスケープの崩れによる構文破壊・注入のリスクを生む         |

## AskUserQuestion の使い分け [MANDATORY]

境界は「議論の内容か、進行上の機械的な確認か」である。

| 場面                                                                        | 手段                                   |
| --------------------------------------------------------------------------- | -------------------------------------- |
| 論点の提示・背景と本質・推奨・議論の選択肢                                  | **地の文**。AskUserQuestion を使わない |
| 進行上の機械的な二者択一（既存記録の再開 / 新規、一括提示へ切り替える件数） | **AskUserQuestion**                    |

地の文にする理由は、選択肢 UI が背景と本質を置く場所を持たず、規範が求める提示順序
（背景 → 本質 → 推奨 → 選択肢）を満たせないためである。議論の中身を選択肢 UI に載せると、
背景抜きで選ばせる提示になる。

**迷ったら地の文にする。** 「機械的だから」を理由に議論の中身を選択肢 UI へ移してはならない。

**自己呼び出しを行わない [MANDATORY]**。`Skill` ツールで本スキル自身を呼ばない。手順の一部として
「本スキルを起動し直す」と書かない。本スキルは多くの議論の起点に置かれるため、自己呼び出しは
暴走ループになりうる。
