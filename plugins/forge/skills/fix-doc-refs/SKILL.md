---
name: fix-doc-refs
description: |
  文書やコードに書かれた参照が実在するかを機械的に検査し、移動先が決まる参照切れを置換する。
  リンク先のファイル・参照リンクの定義・文書 ID の節番号の実在を突き合わせ、
  移動先が索引で決まるものは書き換え、決まらないものは位置つきで報告する。
  文書を削除・移動・改名した後、および参照を大量に書き換えた後に実行する。
  トリガー: "参照切れを直して", "リンク切れを検査して修正", "参照の実在性チェック",
  "文書を消したので参照を確認", "fix doc refs"
user-invocable: true
argument-hint: "[--scan <dir>,...]   # 省略時は文書と配布物を走査する（テストは含まない）"
allowed-tools: Bash, Read, Edit
---

# fix-doc-refs

このスキルは**参照の実在性の検査と、移動先が決まる参照切れの置換だけ**を行う。親が依頼している
他の作業を引き継いではならない。

直せるかどうかは、**その参照が何を指していたかの答えを誰が持っているか**で決まる。

| 答えの所在                               | 直し方                                   |
| ---------------------------------------- | ---------------------------------------- |
| 文書集合（索引から導ける）               | **script が置換して書き戻す**（Phase 3） |
| このセッションで自分（AI）が変更した記憶 | **その記憶で直す**（Phase 4）            |
| どこにも無い                             | 報告で終端する（Phase 4）                |

**索引から導ける置換先を自分（AI）で考えてはならない。** それは script が返す。返らなかった
ものを読解で埋めると、参照は解決するのに指し先が誤っている状態になり、検査がその嘘を保証する
側に回る。参照切れは壊れて見えるが、誤接続は正しく見える。

**ただし、索引が答えを持たないものでも自分（AI）が答えを持っている場合がある。** このセッション
で次のいずれかを行ったのが自分（AI）であれば、答えを持っている。

| このセッションで行った変更         | 自分（AI）が知っていること     |
| ---------------------------------- | ------------------------------ |
| 節・見出しの移動（位置指定の変更） | 移動先の節番号・見出し         |
| ファイルの移動（パスの変更）       | 新しいパス                     |
| ファイル名の変更（改名）           | 新しい名前                     |
| ファイル・節の削除                 | 内容を他へ統合したか、消したか |

削除の場合、参照を差し替えるか消すかは内容の行き先で決まる。他の文書へ統合したのであれば、
自分（AI）はその行き先を知っているので差し替える。内容ごと消したのであれば、参照を残す意味が
無くなったかどうかは利用者の判断であり、報告で終端する。

**根拠にできるのは「このセッションで自分（AI）がその変更を行った」ことだけである。** 参照元や
参照先の記述からの推測、節番号の近さ、残っている節の数は、いずれも根拠にならない。自分（AI）が
変更したかどうかが判別できないなら、変更していないものとして扱う。

## 何を検査するか

参照先が「検査時点で追跡下にあるか」だけを判定する。**git 履歴・他ブランチ・削除済み・未追跡は
実在として扱わない**。参照を辿る読み手が到達できるのは、その時点の追跡下のファイルだけである。

| 検査する参照の形            | 判定                                         |
| --------------------------- | -------------------------------------------- |
| リンク記法                  | 参照元からの相対パスで解決したファイルの実在 |
| 参照リンク（`[x][ラベル]`） | 同一文書に定義行があること、その参照先の実在 |
| 文書 ID の節参照            | 文書の実在と、指した節番号の実在             |

**検査しないもの**: 参照の記法が正しいか、参照先の内容が参照元の主張を裏付けているか、参照先が
変更されたか。いずれも実在性とは別の関心事である。

**リンク記法は Markdown にのみ適用する。** 実装コード・テストから拾うのは文書 ID の節参照だけである
（添字アクセス `d["a"]["b"]` が参照リンクに見えるため）。

### CommonMark 準拠の範囲

何が参照かは CommonMark に従うが、**全体を実装しているわけではない**。次の形は扱わない。

| 扱わない形                                          | 現れる症状                                               |
| --------------------------------------------------- | -------------------------------------------------------- |
| 実体参照（`[a](f&ouml;&ouml;.md)`）                 | 実在しても参照切れとして報告する（**誤検出**）           |
| destination のバックスラッシュ（`[a](foo\bar.md)`） | 解除の可否が規格と違い、別のパスとして引く（**誤検出**） |
| 参照定義の title が閉じないまま空行をまたぐ形       | 定義でない行を定義として拾う（**誤検出**）               |
| リスト・引用の中の参照定義                          | 同上（**誤検出**）                                       |
| ラベル内部の空白の畳み込み                          | `[a  b]` と `[a b]` が別のラベルになる（**誤検出**）     |
| destination・参照定義が行をまたぐ形                 | 参照として拾わない（**見逃し**）                         |
| shortcut reference（`[ラベル]` 単独）               | 定義行が無くても報告しない（**見逃し**）                 |
| autolink とコードスパンが交錯する形                 | 取り違える                                               |
| setext 見出し・コンテナ内の見出し                   | アンカーを判定不能として報告する                         |

**参照を `[表示名](相対パス)` の 1 行で書けば、いずれにも当たらない。** 上の形を使うプロジェクトでは、
報告をそのまま信じず内容を確かめること。

---

## Phase 1: 索引対象の解決

「何を実在する文書とみなすか」は `.doc_structure.yaml` が決める。**パスを推測せず、既存スクリプトに
解決させる**。

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/doc_backend/prepare_advisor_index.py" specs --no-format
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/doc_backend/prepare_advisor_index.py" rules --no-format
```

`status` が `success` でない場合は、`.doc_structure.yaml` の不備として利用者へ報告し、既定値で
補って進めない。

**2 つの応答を連結して Phase 3 へ渡す。** `root_dirs` は 1 category につき複数返る
（`specs` は `design/` / `plan/` / `requirements/` の 3 つを返す）。索引対象は specs と rules の
両方であり、**片方の応答だけを渡すと、もう一方の category の文書が母集団から落ちる**。

- `--index-dirs-json`: 2 つの応答の `root_dirs` を連結した列
- `--index-exclude-json`: 2 つの応答の `exclude` を連結した列（重複は除いてよい）

連結の結果を Phase 3 のコマンドへそのまま書く。件数を自分で数え直したり、代表 1 つに畳んだり
しない。

**`--no-format` を必ず付ける。** 同 CLI は既定で dprint を実行して索引対象の Markdown を整形する
（doc-advisor の `index-docs` が整形後の本文で `body_hash` を打つため）。本スキルが書き換えるのは
置換先が決まった参照だけであり、走査範囲全体が整形されると置換の差分に無関係な変更が混ざる。
整形は commit 時と索引更新時に走るので、検査側で行う必要がない。

この CLI を使うのは、`.doc_structure.yaml` の展開前の `root_dirs` / `patterns.exclude` を返す CLI が
他に無いためである（`resolve_doc_structure.py --type` は展開済みのファイル一覧を返す）。

## Phase 2: 走査対象の決定

参照元（どのファイルに書かれた参照を検査するか）は索引対象と別である。実装コードのコメントにも
文書 ID の節参照が書かれるため、コードも対象に含める。

引数 `--scan` が指定されていればそれを使う。指定が無い場合の既定は次のとおり。

- Phase 1 で連結した `root_dirs` の全件（プロジェクトが文書と認めている範囲）
- 配布物の置き場（プラグインのディレクトリ）

**テストは既定に含めない。** テストは書式そのものを素材として持つため、参照でない文字列
（架空の ID・書式例の節番号）が構造的に多く現れ、参照切れとして誤報告される。テストの中の参照を
検査したい場合は `--scan` で明示的に指定する。

## Phase 3: 検査

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/doc_reference/check_refs.py" \
  --index-dirs-json '<Phase 1 の root_dirs>' \
  --index-exclude-json '<Phase 1 の exclude>' \
  --scan-dirs-json '<Phase 2 で決めた走査対象>'
```

検査を script が行う。**参照を自分で探さない。**

終了コードは、**所見が残ったか**で決まる。

| コード | 意味                                     |
| ------ | ---------------------------------------- |
| `0`    | 所見が無い                               |
| `1`    | 所見が残った                             |
| `2`    | 検査が失敗した（**結果を信頼できない**） |

**`2` を「所見なし」として扱ってはならない。** 検査を通したという事実が何も保証しなくなる。

出力の欄:

| 欄                     | 内容                                   |
| ---------------------- | -------------------------------------- |
| `findings`             | 所見。Phase 4 で扱う                   |
| `out_of_scope_summary` | 解決の対象外とした参照の種別ごとの件数 |
| `indexed` / `scanned`  | 索引した文書数 / 走査した参照元の数    |

**置換は現在この機構に無い。** 所見は Phase 4 の表に従って人が対処する。

<!--
置換（自動修復）について — 実装されるまで有効にしない。

かつてここには `fix_refs.py` を呼ぶ手順があった。索引で移動先が一意に決まる
`moved_link` を、記録した位置で script が差し替えて書き戻す形である。

その実装を撤去した理由は、置換そのものではなく、**参照先を見つける方式が
要件と食い違っていたこと**にある。REQ-023 FNC-002 は「文書 ID の表記」
（位置指定を伴わない裸の ID）を検査対象と定めているが、実装は `DES-001 §3.4`
のように節番号が付いた形しか抽出していなかった。DES-081 §3.3a が定める
**語頭符号による解決**（全文書で一意になる最短の接頭辞を各文書へ割り当て、
前方一致で引く）は、設計に書かれたまま実装されていない。

語頭符号を実装すれば、参照の解決の軸そのものが変わる。その上に載る置換を
先に作り直すと、土台が変わったときに二度作ることになる。したがって順序は
「語頭符号の実装 → 置換の再実装」とし、それまで Phase 3 は検査のみとする。

復元の材料: 撤去したコード（fix_refs.py / ref_normalize.py）とテスト 4 件は
ブランチ backup/work-1-issue-43-before-reset に残っている。位置写像（正規化と
原本の桁の対応）と、範囲が重ならないことを不変条件とする書き戻しは、そこで
一度動いていた。

以下は撤去した手順の原文である。

  python3 "${CLAUDE_PLUGIN_ROOT}/scripts/doc_reference/fix_refs.py" \
    --index-dirs-json '<Phase 1 の root_dirs>' \
    --index-exclude-json '<Phase 1 の exclude>' \
    --scan-dirs-json '<Phase 2 で決めた走査対象>'

  終了コード: 0 = 置換できない所見が無い / 1 = 置換できない所見が残った /
  2 = 検査または置換が失敗した
  出力の欄: applied（置換した参照）/ remaining（置換しなかった所見）/
  errors（置換を試みて当てられなかったもの）
  置換されるのは moved_link だけ（同じ名前の実在パスが 1 件のとき）。
  missing_anchor と missing_section は置換しない——索引は完全一致で引くため、
  この 2 つは「その完全一致が外れた」という所見である。前方一致や番号の近さで
  代わりを探してはならない。
  --dry-run で置換を実行せず結果だけを見られる。

  置換後の再検査 [MANDATORY]:
  置換を 1 件でも行ったら Phase 3 を同じ引数で再実行する。置換前の結果を根拠に
  「直った」と判断しない。再検査で件数が減っていない・別の所見が増えている場合は、
  置換が誤っていたものとして扱い、その旨を報告する。
-->

## Phase 4: 報告

所見を種別ごとに示す。**種別を混ぜない**——対処が異なる。

| 種別              | 意味                                                 | 対処                   |
| ----------------- | ---------------------------------------------------- | ---------------------- |
| `broken_link`     | パスとして解決せず、その名前を持つ文書も実在しない   | 参照の削除・差し替え   |
| `moved_link`      | パスとして解決しないが、その名前を持つ文書は実在する | 実在するパスへ差し替え |
| `missing_anchor`  | 参照先の文書は実在するが、そのアンカーが実在しない   | 自分が変えたなら直す   |
| `missing_label`   | 参照リンクのラベルに定義行が無い                     | 定義行の追加           |
| `missing_doc`     | その文書 ID を持つ文書が実在しない                   | 参照の削除・差し替え   |
| `missing_section` | 文書は実在するが、その節番号が実在しない             | 自分が動かしたなら直す |
| `ambiguous`       | 参照が複数の文書に当たり一意に決まらない             | 参照の表記を長くする   |
| `undecidable`     | 解決規則を持たない、または索引が無く判定できない     | 人が確認する           |

解決の対象外とした参照（外部 URL 等）は所見ではない。`out_of_scope_summary` に種別ごとの件数が
出るので、**所見と混ぜずにその件数を伝える**。

**`undecidable` を所見なしへ畳み込まない。** 判定していないものを「問題なし」と報告すると、
見逃しがそのまま通る。

**新規に作成してまだ `git add` していない文書へのリンクは、参照切れとして報告される。** 索引は
追跡下のファイルから作るため、未追跡の文書は実在に含まれない。`git add` してから検査すれば解消する。

所見について、**このセッションで自分（AI）がその変更（移動・改名・削除）を行ったかを確かめる**。
行っていれば答えを持っているので `Edit` で直す。行っていなければ、参照元の位置・書かれた参照・
なぜ解決できなかったかを示して報告で終端する（利用者の指示を待つ）。

### 直した後の再検査 [MANDATORY]

参照を 1 件でも直したら、**Phase 3 を同じ引数で再実行する**。修正前の結果を根拠に「直った」と
判断しない。再検査で件数が減っていない・別の所見が増えている場合は、修正が誤っていたものとして
扱い、その旨を報告する。

---

## 禁止事項

| 禁止                                               | 理由                                                                                                                  |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| 所見の指し先を推測して書き換える                   | 参照は解決するのに指し先が誤っている状態を作る                                                                        |
| **文字列一致で参照を一括置換する**                 | パスは同一ファイル内で別の参照の部分文字列になりうる。文字列で当てると別の参照を巻き込む。位置を特定して 1 件ずつ直す |
| `missing_section` の節番号を推測して張り替える     | 旧→新の対応は索引に無い。番号の近さは根拠ではない                                                                     |
| 参照を削除して参照切れを消す                       | 依存の記録が失われる。削除は利用者の判断で行う                                                                        |
| 参照先のファイル・見出し・節を作って参照切れを消す | 文書集合そのものの変更であり、置換の範囲外                                                                            |
| 置換前の検査結果を根拠に「直った」と報告する       | 変更を反映した実体で検証していない                                                                                    |
| 終了コード `2` を所見なしとして扱う                | 検査を通した事実が何も保証しなくなる                                                                                  |
| `undecidable` を報告から落とす                     | 判定していないものが「問題なし」に見える                                                                              |
| 走査対象・索引対象のパスを推測して書く             | `.doc_structure.yaml` の解決結果と食い違う                                                                            |
