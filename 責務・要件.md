# forge と doc-advisor の責務・要件

forge と doc-advisor の責務も含めて見直した。責務なので要件と言ってもよい。

## 前提

必須ではない参照は禁止である（[spec_format.md][spec-format]「文書参照の記法」第 1 原則）。（一般）ユーザ文書を禁止にはできないが、我々が開発で使う文書は禁止である。

当然、plugin 文書のルールでは禁止できない。全世界のユーザの文書を禁止しようとすることになるためである。

## 問題の所在

文書の中の文字列に対して、機構は 2 つのことを順に行う。

1. **判別** — その文字列が参照かどうかを決める
2. **特定** — 参照だとして、どの文書を指すかを決める

この 2 段の結果で、状態は 4 つに分かれる。

| ID     | 状態                                                                   | すべきこと   | 位置づけ         |
| ------ | ---------------------------------------------------------------------- | ------------ | ---------------- |
| **S1** | 参照だと判別でき、指す文書も特定でき、**書かれたパスも正しい**         | 何もしない   | **本質的な要求** |
| **S2** | 参照だと判別でき、指す文書も特定できたが、**書かれたパスが誤っている** | **報告する** | **本質的な要求** |
| **S3** | 参照だと判別できたが、指す文書が実在しない                             | **報告する** | **本質的な要求** |
| **S4** | 参照かどうかを判別できない                                             | 追わない     | 解けない見込み   |

**S2 が生じるのは、参照がパスを含む場合だけである。**

**間違っていたら報告する。** パスが誤っていても名前で文書を特定できるため機構としては動くが、[document_style_guide.md][style-guide] §5.1 系統 (3) は「人がクリックで飛べること」を求めており、パスが誤ったままでは人の導線が壊れる。したがって報告はする。

ただし**修正するかどうかは利用者の判断**であり（1-3）、機構が止める理由にもしない。

**要求の中心は S1〜S3 を正しく扱うことである。** S4 に集中すると、解けないものを解こうとして要求が複雑化し、機構が混迷する。S4 は**少なければよい**のであって、0 にする必要はない。

実在ファイルの一覧だけでは S4 は無くならない。一覧は実在するものから作るため、当たらなかったトークンが「消えた文書への参照」なのか「文書名ではない語」なのかを区別する材料を持たない。**これは受け入れる。**

本リポジトリでの実測では、次の 3 種類が S4 に残った。

| ID     | 残るもの                                                 | 例                                             | 機械で決まるか              |
| ------ | -------------------------------------------------------- | ---------------------------------------------- | --------------------------- |
| **N1** | 名前空間付きの他プロジェクトの ID を自分のものと誤認する | `doc-advisor:ADR-002`                          | 決まる（形の問題）          |
| **N2** | 文書が消えたこと自体を述べている文                       | 「**旧** REQ-011 の未確定事項」                | **決まらない**              |
| **N3** | ID を持たない文書の名前が、ふつうの語と一致する          | `frontmatter` `consult` `reviewer` `evaluator` | 決まる（R4 のキーの決め方） |

このうち N2 は文脈の判断であり、機械化できない。参照切れとして報告し、人が「これは記述である」と判断する以外に道がない（実測 1 件）。**この 1 件のために規則を足さない。**

## 0. 共通機能

| ID  | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0-1 | 文書名のみによる参照の検出は、「ハフマン符号化にヒントを得た前方完全一致検索、ただしキーが特殊で、全ファイルで一意に決まる最短長の、前方からのファイル名の一部をキーに使う」方式が適当だと考える                                                                                                                                                                                                                                                                                                                                                    |
| 0-2 | これは script で実装できる                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 0-3 | doc-structure からディレクトリを取得して DIR で動作する。**実在の判定も一覧への追記も、その対象ディレクトリのうち git が追跡するファイルを見る**（作業ツリーに在っても未追跡なら実在に含めない）。SDD フローの途中で修正したファイルリストだけを渡す案は採らない。削除・改名で壊れるのは**触っていないファイルの側**であり、差分だけを走査すると構造的に取りこぼす。速度の問題ではなく正しさの問題である。**実在の判定は、書かれたパスに symlink が含まれる場合、その先の実体まで解決してから行う。字面の一致だけでは実在を確認したことにならない** |
| 0-4 | R4 のキーは保存しない。検索の都度作成する                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 0-5 | 「文書名のみによる参照」だけでなく「すべての参照検出」で、ADR や履歴は対象外とする                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 0-6 | ユーザ文書を禁止にするのは難しいが、基本的に開発文書は「**プロジェクトルートの外を指すパス参照を禁止**、Web URL（配置も含めてリリースされた文書）は許可」。判定は**正規化した結果がプロジェクトルートの内側に収まるか**の一点で行う。絶対パスも `../` による脱出も同じ検査で落ちる。越境 symlink は表記自体がプロジェクト相対なので例外条項を要しない。ID 参照は索引の外では解決しないため、禁止するまでもない                                                                                                                                      |

## 1. forge

| ID  | 内容                                                                                                                                                 |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1-1 | forge は参照リンクの間違いを検出する責務を持つべきだが、単なる文書名による参照は、それが存在しないことは報告するが、それ以上の修正はするべきではない |
| 1-2 | 単なる文書名による参照は、正しい（パス付きの）リンクを書く必要はない。今後それは doc-advisor が参照先を正しく解決してくれるから                      |
| 1-3 | 文書名の間違いはユーザに報告するが、修正するかどうかはユーザの判断である。修正するにしても AI に実施させる可能性が高い                               |
| 1-4 | ただし、参照リンクの間違いを正すことは必要である。その場合でも、script による自動修正まで踏み込むのは無理があると考え直した                          |
| 1-5 | 正しいリンクはファイルを全スキャンして保持できるので、間違っているかどうかは script で判定できるかもしれない                                         |
| 1-6 | が、修正は AI に任せたい。理由は「置換にはさまざまな制約や書き方のバリエーションがある」ため、script ベースでは問題が大きいと考えるから              |
| 1-7 | また、常にこれを実行していればほぼ間違いは検出されないはずで、AI の負荷もユーザの修正待ち時間も少ないはず                                            |

## 2. doc-advisor

| ID  | 内容                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2-1 | doc-advisor は、文書の検索のために文書の参照情報を使う                                                                                                       |
| 2-2 | なので参照の間違いは関知しない（それは forge やユーザの責任である）                                                                                          |
| 2-3 | ただし、マークダウンリンク・リファレンスリンク・文書名のみによる参照は理解しなくてはいけない                                                                 |
| 2-4 | 文書名のみによる参照の検索には「共通」機能が必要である                                                                                                       |
| 2-6 | また、パスの正しさより文書の特定が大事なので、「共通」機能を使えばパスが間違っていても正しいパスは即座にわかる。つまり「共通」機能だけで成り立つ可能性が高い |

## 設計

要件ではなく、上記を実現するための設計である。

### 参照を検出する手段

正しいかどうかではなく、**参照しているかどうかだけ**を検出する手段は次の 4 つである。**どれか 1 つを選ぶのではなく、すべてを走らせて和を取る。** 取りこぼしは回復できないが、余分に拾ったものは読めば分かるからである。

| ID     | 手段                       | 拾うもの                                             |
| ------ | -------------------------- | ---------------------------------------------------- |
| **R1** | 符号形式（正規表現）       | `DES-081` のような ID                                |
| **R2** | ファイル名検索             | `document_writing_rules.md` のような完全なファイル名 |
| **R3** | Markdown の記法            | `[表示名](パス)` などの記法で書かれたもの            |
| **R4** | 区切りを含む部分ファイル名 | `document_writing` のような省略形                    |

フロントマターの関連文書欄は手段のように見えるが、場所が違うだけで実体は R2 または R3 である。

**コードスパン・コードブロック内は判別の対象外とする（例示と見なす）。** 記法の説明として `DES-081` のように書かれた文字列は、実際の参照ではなく例示である。

#### R4 のキーの決め方

**区切り文字は「英数字（Unicode の文字・数字）以外のすべて」と定義する。** 列挙すると必ず漏れる（`-` `_` `%` `ー` `=` `[` `.` … 世界のファイル名は何でも来る）。

ファイル名を区切り文字でセグメントに分け、**先頭から数えて区切りを 1 つ以上含み、かつ一意になる最短のセグメント境界**までをキーにする。

| ファイル名                       | セグメント                             | キー                       |
| -------------------------------- | -------------------------------------- | -------------------------- |
| `document_writing_rules.md`      | document / writing / rules / md        | `document_writing`         |
| `cli_output_formatting.md`       | cli / output / formatting / md         | `cli_output`               |
| `NOTES_open_design_questions.md` | NOTES / open / design / questions / md | `NOTES_open`               |
| `reviewer.md`                    | reviewer / md                          | `reviewer.md`（R2 と同じ） |

一意でなければ次の境界まで伸ばす（`document_writing` が 2 件に当たるなら `document_writing_rules`）。キーが区切りで終わることはなく、語の途中で切れることもない。

**区切りを 1 つ以上含ませるのは、一般語を避けるためである。** `document` だけをキーにすると地の文の「document」に当たる。ただし当たっても実在ファイルへ解決するため報告はされない（S1）ので、これは報告の量を抑えるための調整であって、正しさの条件ではない。

**省略形のうち、区切りを含まないものは拾えない。** `reviewer.md` を `review` と書かれた場合が該当する。これは S4 として残る。

### 判定の段

**発見・特定・パス検査は別の機能である。** 1 つの判定に混ぜると、どの手段で発見したかが状態の判定に影響するように見えてしまう。実際には影響しない。

| ID    | 段       | 機能                                       | 入力                     | 出力                               |
| ----- | -------- | ------------------------------------------ | ------------------------ | ---------------------------------- |
| PH-01 | 発見     | R1〜R4                                     | 本文                     | 参照候補（位置と、書かれた文字列） |
| PH-02 | 特定     | 名前の部分から、指す文書を決める           | 参照候補                 | 指す文書 / 特定できない            |
| PH-03 | パス検査 | 書かれたパスと、特定した文書のパスを比べる | 参照候補と、特定した文書 | パスなし / 一致 / 不一致           |

**PH-02 が見るのは、書かれた文字列のうち名前の部分だけである。ディレクトリの部分は見ない。**

`docs/wrong/foo.md` と書かれていても、PH-02 は `foo.md` だけを見て文書を決める。ディレクトリが誤っていても文書は特定できる。**これが PH-03 を別に置く理由である**——特定できて初めて「正しいパス」が分かり、書かれたパスと比べられる。

引く先は書かれ方で変わる。

| 書かれ方                     | 引く先               |
| ---------------------------- | -------------------- |
| ID（`DES-081`）              | 符号の一覧           |
| ファイル名（`foo.md`）       | 名前の一覧           |
| 省略形（`document_writing`） | 名前の一覧へ前方一致 |

現在実在するものに当たらなければ、かつて実在した名前を引く（後述の補助手段）。そこに当たれば「消えた文書を指している」と分かる。どちらにも当たらなければ「特定できない」である。

PH-03 は PH-02 が成功したときだけ動く。特定できていなければ、比べる相手が無い。

状態は 3 段の結果の組み合わせで決まる。

| PH-01    | PH-02        | PH-03    | 状態   |
| -------- | ------------ | -------- | ------ |
| 発見なし | —            | —        | **S4** |
| 発見     | 特定できない | —        | **S3** |
| 発見     | 特定できた   | パスなし | **S1** |
| 発見     | 特定できた   | 一致     | **S1** |
| 発見     | 特定できた   | 不一致   | **S2** |

**どの手段（R1〜R4）で発見したかは、状態の判定に関係しない。** 手段が違うのは PH-01 だけで、PH-02・PH-03 は同じ機能を通る。

R1 と R4 が拾うのは名前だけなのでパスを持たず、PH-03 は必ず「パスなし」になる。R2 と R3 はパスを伴いうる（`docs/wrong/foo.md` のような字面、`[表示名](パス)` の記法）。

### 保存するもの・しないもの

**保存するのは、doc-structure の対象ディレクトリに実在した（かつて実在したものを含む）ファイル名と、そこから切り出した符号である。本文から見つけたものではない。**

| 対象                                                  | 保存   | 理由                                                    |
| ----------------------------------------------------- | ------ | ------------------------------------------------------- |
| 実在したファイル名から切り出した**符号**（`DES-048`） | する   | 現在の一覧との差分が判別の材料になる                    |
| 実在した**ファイル名**（`session_format.md`）         | する   | 同上。R2 には必須                                       |
| R3 が見つけたもの                                     | しない | 記法が毎回判別するので、過去の結果は要らない            |
| R4 のキー                                             | しない | 文書集合の関数。1 文書増えると伸びる（0-4）             |
| 符号形式（正規表現）                                  | 別扱い | 判別の規則であり検出状態ではない。設定として持つ（U-1） |

**本文由来のものを混ぜてはならない。** 本文の打ち間違い（`DES-999`）を保存すると、実在したことのない名前が「消えた文書」として恒久的に扱われる。R1 は正規表現が本文から直接判別できるので保存は冗長であり、R2 は実在した名前しか探せないので本文由来では成り立たない。

### 補助手段: かつて実在した名前

保存した一覧のうち**現在は実在しないもの**で検索する。R1〜R4 と並ぶ 5 つ目ではなく、**手段ごとに必要性が違う補助**である。

| ID | 過去の一覧が要るか | 理由                                                                       |
| -- | ------------------ | -------------------------------------------------------------------------- |
| R1 | 補助               | 正規表現は消えた ID も判別できる。class で絞る場合のみ、全消しに備えて要る |
| R2 | **必須**           | 現在の一覧に在る名前しか探せない。消えた名前は過去の一覧でしか判別できない |
| R3 | 不要               | 記法が判別するので、実在に依存しない                                       |
| R4 | **必須**           | R2 と同じ理由                                                              |

**副産物として、S4 の一部が S3 へ移る。** 保存を「かつて実在した名前」に限るため、本文に残る名前がそれに当たれば「消えた文書への参照」だと確定できる。追加の規則は要らない。ただし移るのは N1〜N3 ではなく、表に挙げていない別の種類（消えた文書を名前だけで指した参照）である。

### 置き場

**forge がプロジェクトルートへ書くディレクトリを `.forge/` として新設する。** 現状 forge はルート直下に `.doc_structure.yaml` を単独で置いているが、将来的にはこれも `.forge/` へ移す。

```
.forge/doc-references/known-ids.json     これまでに実在した符号の一覧
.forge/doc-references/known-names.json   これまでに実在したファイル名の一覧
```

**保存するのは「これまで実在したことがある全名前」であって、「消えた名前」ではない。** 消えた名前は毎回 `保存分 − 現在の一覧` で導出する。**前回一覧との差分だけ**を保存する形は採れない——直前 1 回分しか残らず、**何回か前に削除された文書への参照を検出できなくなる**。

種別ごとに分ける。必要性が違い（上表）、混ぜると R2 の分だけで足りる場面でも全部を読むことになる。R3 の分は保存しない。

**commit する。** 履歴が本体であり派生物ではない。`.gitignore` へ入れない。ローカルに置くと人ごと・CI と手元で判別結果が変わる。

**doc-advisor はこの一覧を持たない。** doc-advisor の責務は解決であり（2-1 / 2-6）、存在しない文書には解決できない。共通にするのは符号形式（判別の規則）だけで、過去の一覧は forge 固有である。2 箇所に持つと「かつて何が在ったか」が食い違い、同じ文書に別の判定を返す。

### 更新の仕方

検査が終わったら、現在実在するファイルの名前・符号を一覧へ**追記する**。**一覧から行を減らさない**——削除された名前を落とすと、その文書への参照を検出できなくなる。したがって一覧が変わるのは、文書が新しく作られたときと、刈り込みのときだけである。順序を固定して書き出し、内容が変わったときだけ差分が出るようにする。

**削除は差分に現れない。** 名前が残り続けるためである。削除の記録は git が持っているので、二重には持たない。

**初回は保存分が無い。** 新規クローン・新規プロジェクトでは、その時点で実在するものだけが一覧になる。過去に消えた文書への参照は検出できない。degrade する仕様として扱う。

### 刈り込み

一覧は文書を作るたびに増えるが、規模はこれまでに作られた文書数に比例するだけで問題にならない。

**混入は追記の基準で減らす。** 一覧へ入れるのは git が追跡しているファイルだけとする（0-3）。commit する前に作って消した `FNC-013_x.md` は一覧に入らない。誤混入の大半は「試しに作って消した」型なので、これで大きく減る。commit まで到達した削除は本物の削除であり、一覧に残るのが正しい。

**残る混入には人が対処する。** commit した後に消した誤ファイルは一覧に残り、たとえば `FNC-013` なら本文の 19 箇所が参照切れとして報告され続ける。人が該当行を削除できる経路を残す。行の削除は一覧ファイルへの commit になるため、レビューできる形で履歴に残る。

## 未決定項目

### U-1. 符号形式（設定）をどう持つか

R1 の正規表現。AI が実在ファイル一覧から導出し、人が承認して保存する、というところまでは決まっている。決まっていないのは次。

- **グループの契約** — `id`（識別子そのもの）を必須、`class`（同種の文書が共有する部分）を任意とする案。`class` は `DES` / `DESIGN:` に当たり、adr-tools の `0001-title.md` では存在しない
- **`class` が無い形のときの扱い** — 参照切れ検出の観測単位が無くなる。「この形では参照切れ検出を提供しない」と機構が明示する案
- **境界の守りをどちらが持つか** — `:` を含む形があるため script は文字クラスを決め打ちできない。設定（正規表現）側に持たせる案
- **置き場** — `.forge/` 配下か、`.doc_structure.yaml` へ足すか

### U-2. 符号形式を導出する手順のうち、最後の段で何が言えるか

導出は 5 段（実在ファイル一覧 → 先頭部の字種分類 → 分布の集計 → AI が正規表現を書く → 全文書の本文に当てて人に見せる）。1〜3 と 5 は script、4 だけが AI。

最終段で人に見せるのは 3 つの数字と実例である。

| 欄                   | 人が判断すること   |
| -------------------- | ------------------ |
| 解決した             | 参照として妥当か   |
| 参照でないと判定した | 捨ててよいか       |
| 参照切れの候補       | 本当に壊れているか |

**ただしこれで言えるのは「拾ったものが妥当か」までである。拾い漏らしが無いことは示せない。** 全文書を人が 1 件ずつ読まない限り証明できず、大規模なプロジェクトでは不可能である。この段を「検証」と呼ぶかどうかを含めて未決。

### U-3. 0-5（ADR・履歴を対象外にする）をどう表現するか

`.doc_structure.yaml` の `patterns.exclude` は「除外するディレクトリ名」しか受け付けない。ADR は `design/` に設計書と同居しているため、現在の設定形式では表現できない。U-1 の `class` が決まれば `class` 単位で除外できる。

あわせて、ADR を丸ごと対象外にするのが妥当かも未決。ADR は現役の規範として読まれ、編集もされる。

### U-4. 「共通」機能の置き場

forge と doc-advisor は別リポジトリで、doc-advisor は `.doc_structure.yaml` を読まない。同じ判別規則を 2 箇所に実装すると、同じ文書に別の判定を返す。

### U-5. 修正を AI に任せる場合のガード（1-6）

AI に置換させると、文字列一致による巻き込みと参照定義行の破壊が起こりうる（いずれも実際に起きた）。`replace_all` の禁止と、修正後の再検査の義務化を規範として明記するか。

### U-6. CommonMark 準拠の範囲と、その検出方法

**R3 だけが外部仕様に従う義務を負う。** R1・R2・R4 は自前の規則なので、外れても自分の設計問題として直せる。R3 は CommonMark という正本があり、外れれば準拠違反になる。

決まっていないのは次。

- **どこまで従うか** — 全部は実装しない。追随する規定と追随しない規定を宣言する必要がある。宣言しないと「仕様に従っている」が何を保証するのか読み手に伝わらない
- **適合をどう測るか** — 公式の用例集（`spec.json`）に当てる。ただし用例の期待値は**変換後の HTML** であり、抽出が返す destination とは表記が異なる（属性値の HTML エスケープ・描画時の percent-encode）。**期待値の側だけを戻す**必要がある——両辺へ同じ処理を掛けると、実体参照を解決していないといった欠陥が打ち消されて見えなくなる
- **非対応の形の混入をどう検出するか** — 「実装しない」と決めた根拠が「本リポジトリに 0 件」である以上、誰かが書いた時点で根拠は失われる。混入を機械が知らせる仕組みが要る
- **doc-advisor との一致** — 向こうは CommonMark 解析器（mistletoe）を vendoring 済み。R1・R2・R4 は規則を揃えれば一致するが、**R3 は「どのパーサを使うか」まで揃えないと一致しない**。forge は標準ライブラリのみ、doc-advisor は外部ライブラリありで前提が違う（U-4 と重なる）

なお `[foo]` 単独（shortcut reference）は、CommonMark では定義 `[foo]: パス` がある場合にのみ参照である。定義が無いものを参照として扱うかどうかは、規格の外の判断になる。

[spec-format]: plugins/forge/docs/spec_format.md
[style-guide]: plugins/forge/docs/document_style_guide.md
