# .claude/.doc-advisor/toc/rules-6c621d1a0513/toc.yaml
# Document Search Index for key: rules
# Auto-generated by doc-advisor index-docs - Do not edit directly

metadata:
  name: Document Search Index (rules)
  key: rules
  generated_at: 2026-09-03T06:36:03Z
  file_count: 7

docs:
  docs/rules/cli_output_formatting.md:
    title: CLI Output Formatting Guidelines
    purpose: Defines color-coding conventions and output patterns for shell scripts and CLI tools
    content_details:
      - RED/GREEN/BLUE/YELLOW/NC ANSI color code definitions
      - "Color usage table: green for success/headers, blue for config values/paths, yellow for user commands to run, red for warnings/errors"
      - Header/footer banner pattern using GREEN with echo -e
      - Configuration value display pattern using BLUE
      - Warning message pattern using RED
      - Next steps/command pattern using YELLOW
      - Success message pattern (PASS/GREEN)
      - Error message pattern (FAIL/RED)
      - Requirement to use echo -e for escape sequence interpretation
      - Requirement to always reset color with NC after colored text
    applicable_tasks:
      - Shell script CLI output implementation
      - Adding colored terminal output to a tool
      - Reviewing shell script output formatting consistency
    keywords:
      - echo -e
      - ANSI color codes
      - RED GREEN BLUE YELLOW NC
      - CLI output
      - shell script formatting
      - color coding
      - header banner
      - PASS FAIL
  docs/rules/document_writing_rules.md:
    title: Document Writing Rules
    purpose: Establishes document writing rules for consistency across rules, specs, skills, and plugin documentation; covers heading conventions, terminology, structure, and formatting standards.
    content_details:
      - Heading and tag conventions with MANDATORY/CRITICAL/IMPORTANT tags
      - Terminology unification and standardized English phrases
      - Document structure requirements by type (workflow, rules, notes)
      - Metadata frontmatter format (name, description, applicable_when)
      - Formatting conventions (bold, backticks, lists, tables)
      - Document reference notation and markdown links
      - Document properties and role separation (rules vs specs vs plugins)
      - Current state documentation principle without migration narratives
      - Abstract examples and placeholder naming instead of project-specific terms
      - Scope applicability across rules, specs, and skills directories
    applicable_tasks:
      - Document authoring and editing
      - Document review and consistency checking
      - Metadata frontmatter creation
      - Writing SKILL documentation
      - Project rules and specifications development
    keywords:
      - document-writing-rules
      - frontmatter
      - heading-levels
      - terminology-unification
      - markdown-formatting
      - document-structure
      - MANDATORY
      - reference-links
      - tag-placement
      - scope-applicability
  docs/rules/forge_doc_access_principle.md:
    title: forge Document Access Principle
    purpose: "Defines the design principle for how forge internal components (SKILL, agent, scripts) access documentation via two routes: direct reference to built-in docs or query-based discovery of project specs and rules."
    content_details:
      - "Two access routes: Route A (direct reference to built-in docs), Route B (query-based discovery of project specs/rules)"
      - "Route A scope and restrictions (same skill can reference own docs, cross-reference of other skills' docs prohibited)"
      - Direct reference mechanism using ${CLAUDE_PLUGIN_ROOT} and ${CLAUDE_SKILL_DIR} paths
      - Query mechanism using /forge:query-db-specs and /forge:query-db-rules
      - Rationale for two routes (built-in docs stable with fixed paths, project docs need dynamic discovery)
      - "Anti-patterns: querying built-in docs, referencing project docs from distributed code, hard-coding project paths"
      - Document escalation rule (shared documents move to plugins/forge/docs)
      - Prohibition of session_dir/refs.yaml-based file passing
      - External usage via /forge:query-forge-rules (user-facing)
      - Related implementation in query-db-specs, query-db-rules, rules-query-worker agents
    applicable_tasks:
      - Writing forge SKILL documentation
      - Implementation of forge agents and scripts
      - Document architecture and access pattern design
      - Code review of forge internal components
      - Plugin documentation organization and maintenance
    keywords:
      - forge-document-access
      - direct-reference
      - query-based-discovery
      - CLAUDE_PLUGIN_ROOT
      - query-db-specs
      - query-db-rules
      - built-in-docs
      - anti-patterns
      - document-escalation
      - session_dir
  docs/rules/implementation_guidelines.md:
    title: Implementation Guidelines
    purpose: Defines rules for plugin script language selection, testing, SKILL.md structure, asset placement, distribution boundaries, and prohibiting reflexive fallbacks or unauthorized version file edits
    content_details:
      - Python vs Bash script language selection criteria table, with PyYAML as the sole allowed external dependency
      - "Test requirement matrix by asset type: plugins/*.py mandatory, plugins/*.sh recommended, SKILL.md exempt, .claude/ excluded"
      - Prohibition on inline scripts in SKILL.md; logic must live in external script files invoked via ${CLAUDE_PLUGIN_ROOT}
      - "Asset placement rule: skills/{skill}/scripts vs plugins/{plugin}/scripts, skills/{skill}/docs vs plugins/{plugin}/docs"
      - "Prohibition on referencing another skill's skills/{skill}/ directory directly; promote shared assets to plugin-common location instead"
      - "ADR placement rule: docs/specs/**/design/ADR-{NNN}_{topic}.md alongside the design doc"
      - "Distribution boundary rule: user-facing plugins/ files (SKILL.md, agents, commands, bundled docs) must not reference docs/ project documents by path or spec ID"
      - "Two allowed placements for norms referenced from distribution artifacts: inline in the referencing source, or a separate doc under plugins/<plugin>/docs/"
      - Prohibition on script parsers for AI-interpreted input (command arguments); AI must interpret directly with AskUserQuestion for ambiguity
      - Reflexive fallback prohibition and version-related file edit restriction (plugin.json, marketplace.json, README version table, CHANGELOG, git tags) with /forge:update-version as sole legitimate route
    applicable_tasks:
      - Plugin script language selection between Python and Bash
      - Writing unit tests for plugins/ Python scripts
      - Refactoring SKILL.md to extract inline scripts
      - Deciding script or doc placement between skill-specific and plugin-common locations
      - Reviewing cross-skill asset references for improper coupling
      - Reviewing distribution artifacts for prohibited docs/ references
      - Reviewing edits to version-related files (plugin.json, CHANGELOG, README)
      - Designing error handling and avoiding reflexive fallback logic
    keywords:
      - PyYAML
      - SKILL.md
      - CLAUDE_PLUGIN_ROOT
      - ADR-055
      - update-version
      - reflexive fallback
      - AskUserQuestion
      - distribution boundary
      - version-related files
      - plugin.json
  docs/rules/skill_authoring_notes.md:
    title: SKILL.md Authoring Notes
    purpose: Defines rules for authoring/editing SKILL.md frontmatter, the inheritance-only execution model, defense-in-depth layers, argument handling, and mandatory AskUserQuestion usage for user confirmations
    content_details:
      - "Frontmatter field reference table: name, description, user-invocable, argument-hint, disable-model-invocation, allowed-tools, disallowed-tools, context, agent"
      - "Prohibition on fork-type SKILL (context: fork is deprecated); all SKILL must be inheritance-type, with isolation handled via custom Agent"
      - "Defense-in-depth layer table: A. Agent boundary, B. Role constraints, C. allowlist, D. physical deny via permissions.deny"
      - "Common misconception corrections: allowed-tools/tools is an allowlist not a denylist; disallowed-tools resets once the user sends the next message"
      - "Naming convention prefixes: query-* for search, create-*/build-* for writes, start-* for staged workflows"
      - Self-recursion prohibition for query-db-rules/query-db-specs/query-forge-rules to avoid infinite-loop harness hangs
      - "Agent background launch discipline requiring ScheduleWakeup(stop: true) cancellation immediately upon receiving the task-notification"
      - Dependent skill existence check via the available-skills system reminder as a pre-detection pattern
      - "SKILL.md split criteria table: workflow instructions stay inline, content/templates/detailed rules over 500 lines move to external files"
      - Mandatory AskUserQuestion usage for all user confirmations, with a narrow exception for discussion-content presentation in argumentative skills
    applicable_tasks:
      - Authoring or editing SKILL.md frontmatter fields
      - Deciding between a custom Agent and an inherited SKILL for isolation needs
      - "Reviewing a SKILL.md for deprecated context: fork usage"
      - Implementing defense-in-depth layers for a new skill
      - Writing self-recursion guards for query-* skills
      - Implementing background Agent launch with ScheduleWakeup fallback
      - Deciding SKILL.md content placement between inline and external files
      - Replacing plain-text user prompts with AskUserQuestion
    keywords:
      - "context: fork"
      - 継承型 SKILL
      - カスタム Agent
      - allowed-tools
      - disallowed-tools
      - ScheduleWakeup
      - AskUserQuestion
      - available-skills
      - ADR-002
      - disable-model-invocation
  docs/rules/skill_launch_paths_definitions.md:
    title: SKILL / Agent / Subagent Launch Path Definitions
    purpose: Defines the 5 delegation launch paths in Claude Code (inherited SKILL, fork SKILL, general-purpose Agent, custom Agent, Bash subprocess) and establishes their official short names to prevent terminology confusion
    content_details:
      - Launch path classification table of 5 types (継承型 SKILL, fork 型 SKILL, 汎用 Agent, カスタム Agent, Bash subprocess) with launch tool, procedure-document location, parent-context, input, output, and adoption-status columns
      - "fork-type SKILL deprecation rationale citing anthropics/claude-code structural bugs #18394, #34164, #60720, #55592 (context: fork ignored, $ARGUMENTS substitution failure, silent output loss, infinite recursion)"
      - Naming usage rule mandating official short names over the ambiguous term subagent
      - Term-usage unification table mapping ambiguous subagent references to the correct short name (fork型SKILL / 汎用Agent / カスタム Agent)
      - "subagent_type value domain definition: general-purpose/Explore/Plan/statusline-setup for 汎用Agent, agents/<name>.md-defined names like forge:reviewer or doc-advisor:toc-updater for カスタム Agent"
      - "Invalid subagent_type value examples: SKILL/slash-command names (forge:review, /anvil:create-pr), file paths (plugins/forge/skills/review/SKILL.md)"
      - "Essential relationship notes: 汎用Agent vs カスタム Agent distinguished by where the procedure document lives (prompt each call vs fixed system prompt)"
      - "Historical equivalence note: fork型SKILL and カスタム Agent share the same isolated-context-plus-predefined-role model, differing only in launch tool and definition file location"
      - Reference to the machine-readable term subset docs/rules/skill_launch_terms.toml
    applicable_tasks:
      - Writing or reviewing SKILL.md launch path terminology
      - Choosing the correct subagent_type value when launching an Agent tool
      - Distinguishing custom Agent from general-purpose Agent
      - Reviewing design documents for correct launch path short names
      - "Auditing SKILL.md or agents/*.md for deprecated context: fork usage"
      - Resolving ambiguous subagent terminology in project documentation
    keywords:
      - 継承型 SKILL
      - fork 型 SKILL
      - 汎用 Agent
      - カスタム Agent
      - Bash subprocess
      - subagent_type
      - "context: fork"
      - skill_launch_terms.toml
      - general-purpose
      - COMMON-DES-001
  docs/rules/version_migration_design.md:
    title: Version Upgrade and Migration Design Rules
    purpose: Defines design principles (pipeline, idempotency, forward compatibility) and anti-patterns for implementing safe multi-step version migrations across config files, Swift models, and databases.
    content_details:
      - "Anti-pattern: reading old-version data directly without migration causes missing keys to resolve to None"
      - "Anti-pattern: passing fixed original data to every migration step instead of chaining outputs (found in DocAdvisor v4.x multi-step migration)"
      - "Pipeline principle: each migration function takes the previous step's output as input, enabling safe skip-version migration (v4 to v6)"
      - "Single Source of Truth principle: manage version number in one place and embed it in the data itself (self-describing)"
      - "Idempotency principle: migrations must produce the same result when applied twice (e.g. ALTER TABLE ADD COLUMN IF NOT EXISTS)"
      - "Forward compatibility principle: migration functions must preserve unknown keys instead of deleting them"
      - "Applied-history persistence principle: record which version has been applied for re-run prevention and diagnostics"
      - "YAML/JSON migration pattern: MIGRATIONS registry table and apply_migrations() pipeline function"
      - "Swift Codable versioned struct pattern: per-version structs with migrateToLatest() dispatch by version number"
      - "SQL migration pattern: numbered migration files (Rails/Flyway/Alembic style) with schema_migrations history table and transactional apply_db_migrations()"
    applicable_tasks:
      - Designing config/schema version migration logic
      - Reviewing multi-step (version-skipping) migration implementations
      - Implementing idempotent SQL schema migrations
      - Implementing Swift Codable versioned struct migrations
      - Designing .doc_structure.yaml version upgrade and migration
      - Pre-release version upgrade checklist review
    keywords:
      - migration
      - pipeline principle
      - idempotency
      - Single Source of Truth
      - forward compatibility
      - MIGRATIONS
      - apply_migrations
      - schema_migrations
      - doc_structure_version
      - NSMigrationPolicy
