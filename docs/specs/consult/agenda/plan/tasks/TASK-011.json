{
  "task_id": "TASK-011",
  "title": "consult SKILL.md を新CLI呼び出しへ書き換える",
  "priority": 70,
  "status": "pending",
  "design_id": "DES-075",
  "depends_on": [
    "TASK-009"
  ],
  "group_id": null,
  "build_check": "per_task",
  "description": [
    "【2026-08-27 再改訂】TASK-011着手当初（下記【計画時の前提修正】）の作業は既に完了し、consult SKILL.mdはagenda_store.pyの5コマンド契約（start/record/next/pending/finish）へ全面統合済みである。統合直後のレビューで『review委譲時に固定パス・固定識別名を使う分岐が実装されていない』欠陥が見つかり、この欠陥を機に新設された DES-078（consult対話進行フロー設計書）が review起点/consult起点の分岐を正本化した。本タスクの残作業は、この分岐（DES-078 §3・§3.1）をconsult SKILL.mdへ実装することに限定される",
    "【計画時の前提修正（当初完了済み分）】consult SKILL.mdの現状（TASK-011着手時点）はagenda機構への統合が一切存在しない（旧CLI呼び出しも無い。Markdown討議ファイルをWriteツールで直接手書きする、agenda機構導入前の設計のまま）。本タスクは『旧CLI呼び出しを新CLI呼び出しへ置換する』のではなく、agenda_store.pyの5コマンド契約（start/record/next/pending/finish）への統合をゼロから設計・実装するタスクである。以下はその統合内容",
    "Phase 2.1「既存ファイルの確認」: `.claude/.temp/consult/` のls確認を、agenda.json（`.claude/.temp/consult/${CLAUDE_SESSION_ID}/agenda.json`相当）の存在確認・中断中の記録の再開可否判断に置き換える",
    "Phase 2.2「新規作成」: 討議ファイルをWriteツールで直接書く方式を廃し、候補JSON（structural_judgment.note + items[] + config.item_fields/severity_field）をWriteツールで一時ファイルへ書いてから `agenda_store.py start` を1回呼ぶ手順に置き換える。候補ファイル命名は${CLAUDE_SESSION_ID}ベースとし、start-implement/SKILL.md 4.3節の前例（Writeツールでの一時ファイル書き込み→Bashで1回実行）に倣う",
    "Phase 2.2完了直後、DES-075 §2「初回表示はconsultが能動的に開く」に従いBashで`open`コマンドを実行しagenda.htmlをブラウザで開く手順を追加する",
    "Phase 3「アジェンダの提示」: アジェンダ表の出所を、agenda_store.pyの`next`/`pending`呼び出し結果、またはagenda.json自体の読み取りに置き換える",
    "Phase 4「進行」手順5「決着したら討議ファイルを更新する」を、record呼び出し2回（背景・本質が分かった時点で1回、決着時にもう1回）に分割する。分かった時点で都度記録することを明記する",
    "「議論の途中で論点が動いたとき」節を、decision.outcomeに自由文（例:\"取り下げ\"）を書くだけの通常のrecord呼び出しへ置き換える。特別なコマンドではないことを明記する",
    "Phase 5「終了」: agenda_store.pyの`finish`呼び出しに置き換える。未対応項目が残る場合は削除されず`remaining_count`が返るため、その値をそのまま「未判断のまま残った件数」として提示する",
    "自由記述文をシェル文字列に直接埋め込まない理由（1文）を明記する",
    "【残作業（本改訂の対象）】DES-078 §3.1: consult SKILL.mdのPhase 0・Phase 1・Phase 2.2に、起点判定（review委譲直後の文脈か、利用者から直接依頼を受けた文脈か）を明示的な手順として持たせる。判定はAI自身の文脈認識で行い、外部から渡される構造化フラグは新設しない",
    "【残作業】review起点と判定した場合、Phase 1（論点の抽出）を経由せず、review本体から渡された結合済みの所見配列（reviewer所見とevaluator判定を結合したもの。DES-066 §3.10a）をそのまま items[] として用いる",
    "【残作業】review起点の場合、record先パスをDES-075 §7の固定値に切り替える: `.claude/.temp/review/agenda.json`（固定パス）、識別名`review`（親ディレクトリ名固定）。consult起点（`${CLAUDE_SESSION_ID}`ベース）と分岐する",
    "【残作業】review起点の場合、structural_judgment.noteの材料は「同型の指摘の繰り返し・複数指摘が同一の土台に由来していないか」（reviewer所見群を見て判断）とし、consult起点の「consult自身が立てた論点群を見て判断する」と書き分ける（DES-078 §3表）",
    "【残作業】review起点の場合、config.item_fields/severity_fieldにreviewer/evaluatorが確認・訂正した重要度をそのまま反映する（consult:REQ-017 FNC-002）。consult起点は`item_fields: []`, `severity_field: null`のまま変更しない"
  ],
  "acceptance_criteria": [
    "consult SKILL.mdに--set・status_vocabulary・set-current・record-structural-judgmentという旧CLIの語がgrepで一切ヒットしない",
    "start/record/next/pending/finishの5コマンドのみが呼び出されている",
    "consult SKILL.mdのPhase 0・Phase 1・Phase 2.2に、review委譲文脈かどうかを判定する明示的な手順が存在する（DES-078 §3.1）",
    "review起点の分岐を辿った場合に`.claude/.temp/review/agenda.json`（固定パス・固定識別名`review`）が使われることが手順として明記されている（DES-075 §7）",
    "review起点の分岐を辿った場合、Phase 1の論点抽出を経由せず、渡された結合済み配列をそのままitems[]として使う手順になっている"
  ],
  "required_reading": {
    "strategy_doc": "docs/specs/consult/agenda/plan/agenda_strategy.md",
    "design_docs": [
      "docs/specs/consult/design/DES-078_consult_dialogue_flow_design.md",
      "docs/specs/consult/agenda/design/DES-075_agenda_mechanism_design.md",
      "docs/specs/consult/agenda/design/DES-077_agenda_display_design.md",
      "docs/specs/forge/design/DES-066_review_body_design.md"
    ],
    "requirement_docs": [
      "docs/specs/consult/requirements/REQ-017_consult_skill.md"
    ],
    "rule_docs": [
      "docs/rules/forge_doc_access_principle.md",
      "docs/rules/implementation_guidelines.md",
      "docs/rules/skill_authoring_notes.md",
      "docs/rules/skill_launch_paths_definitions.md"
    ],
    "reference_code": [
      "plugins/forge/skills/review/SKILL.md",
      "plugins/forge/skills/start-implement/SKILL.md"
    ],
    "additional": [
      "plugins/forge/skills/consult/SKILL.md",
      "plugins/forge/docs/consult_principles_spec.md"
    ]
  },
  "scope_in": "plugins/forge/skills/consult/SKILL.mdへ、agenda_store.pyの5コマンド契約統合（完了済み）に加えて、DES-078 §3・§3.1が定めるreview起点/consult起点の分岐をPhase 0・Phase 1・Phase 2.2へ実装すること",
  "scope_out": [
    {
      "item": "旧形式Markdown討議ファイルの削除",
      "owner_task_id": "TASK-012",
      "reason": "TASK-011に依存する後続タスク（計画上分離。本タスク完了後の後始末）"
    },
    {
      "item": "combine_findings_and_evaluations.py（reviewer所見とevaluator判定の結合スクリプト）の実装",
      "owner_task_id": "未計画（別途review_plan.jsonで対応予定）",
      "reason": "review本体の所見結合ロジックであり、consult側の起点分岐とは関心事が別（DES-066 §3.10a）"
    }
  ],
  "implementation_instructions": "consult SKILL.mdはagenda_store.pyの5コマンド契約（start/record/next/pending/finish）への統合は既に完了している（Phase 2.1・2.2・3・4・5が新CLIを使用）。残作業はDES-078 §3・§3.1が定めるreview起点/consult起点の分岐のみである。\n\n【起点判定の追加（Phase 0・Phase 1冒頭）】\nconsultは継承型SKILLであり、review本体を実行しているのと同じ会話がconsultの振る舞いへ切り替わるだけである（DES-066 §3.11）。Phase 0（モードの確定）またはPhase 1（論点の抽出と検証）の冒頭に、次の判定手順を明示的に追加する: 「直前にreviewer/evaluatorとの往復を行い、結合済みの所見配列（reviewer所見とevaluator判定を結合したもの。DES-066 §3.10a）を受け取っている場合はreview委譲文脈（review起点）である。そうでなく利用者から直接依頼を受けた場合はconsult起点である」。この判定はAI自身が会話の直前文脈から行うものであり、外部から渡される構造化フラグや新しい引数を追加してはならない（DES-078 §3.1が明示的に禁じている。ファイル名・パスの違いは分岐の結果であって原因ではない）。\n\n【review起点の分岐】\n1. Phase 1「論点の抽出と検証」を経由しない。review本体から渡された結合済みの所見配列をそのまま`items[]`として用いる（consult自身が論点を新たに立てる工程を経由しない。DES-078 §3表1行目）\n2. Phase 2.1「既存ファイルの確認」・Phase 2.2の`agenda_store.py start`呼び出しで、`--path`を`.claude/.temp/review/agenda.json`（固定パス）に切り替える。consult起点の`${CLAUDE_SESSION_ID}`ベースのパスは使わない（DES-075 §7「review起点」の行。親ディレクトリ名が固定識別名`review`として`agenda_store.py`により機械的に導出される）\n3. `structural_judgment.note`の材料は「同型の指摘の繰り返し・複数指摘が同一の土台に由来していないか」（reviewer所見群を見て判断する）とする（DES-078 §3表3行目）\n4. `config.item_fields`/`config.severity_field`は、reviewer/evaluatorが確認・訂正した重要度をそのまま反映する値（`item_fields: [\"severity\"]`, `severity_field: \"severity\"`）を渡す。consult起点（`item_fields: []`, `severity_field: null`）とは異なる値になる（consult:REQ-017 FNC-002、DES-078 §3表4行目）\n\n【consult起点の分岐】\n既存の実装（Phase 1で論点を立てる・`${CLAUDE_SESSION_ID}`ベースのパス・`item_fields: []`）をそのまま使う。変更不要。\n\nPhase 2.1「既存ファイルの確認」も、review起点では`.claude/.temp/review/agenda.json`の存在確認、consult起点では`.claude/.temp/consult/${CLAUDE_SESSION_ID}/agenda.json`の存在確認、と分岐する手順として明記する。\n\n自由記述文をシェル文字列に直接埋め込まない理由の1文は既存のまま維持する（変更不要）。",
  "verification": {
    "build": "required",
    "build_reason": null,
    "tests": "skipped",
    "tests_reason": "SKILL.mdはテスト困難なため例外（CLAUDE.md「Testing」節）。grep・構文確認・手動シナリオ確認で代替する"
  }
}
