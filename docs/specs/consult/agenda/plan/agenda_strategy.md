# agenda 機構 実装戦略

`/forge:start-plan` の実装戦略フェーズ（[DES-027](../../../forge/design/DES-027_plan_strategy_phase_adr.md)）に基づく、agenda 機構への移行戦略。実装完了後に削除する ephemeral 文書であり、恒久文書ではない。

## メタデータ

| 項目     | 値                                                                                                             |
| -------- | -------------------------------------------------------------------------------------------------------------- |
| feature  | consult/agenda                                                                                                 |
| 関連設計 | [DES-075](../design/DES-075_agenda_mechanism_design.md), [ADR-076](../design/ADR-076_agenda_storage_format.md) |

## アプローチ選択

**フィーチャースライス**（新機構を先に単体で成立させ、その後に既存呼び出し元を1つずつ縦断的に繋ぎ替える）を採用する。

理由: `agenda_store.py` / `agenda_render.py` は `consult` の既存実装から独立して動作を確認できる（CLI 単体でinit→update→render の一連が完結する）。先に新機構だけを動作確認してから呼び出し元（`consult` SKILL.md）を繋ぎ替える方が、繋ぎ替え作業中に新機構自体のバグを疑う必要がなくなり、切り分けが容易になる。

## フェーズ分割

| フェーズ | 内容                                                                                                     | 対応する DES-075 の節 |
| -------- | -------------------------------------------------------------------------------------------------------- | --------------------- |
| 1        | `agenda_store.py` / `agenda_render.py` / `agenda_schema.py` の実装（CLI 単体で動作確認可能な状態にする） | §3, §4, §5, §6, §8    |
| 2        | `consult` SKILL.md の書き換え（下記「置き換え対応表」）                                                  | §9.1（本文書へ移設）  |
| 3        | `discussion_file_template.md` の表示テンプレートへのリライト                                             | §9.2（本文書へ移設）  |
| 4        | 既存記録の破棄（FNC-010）                                                                                | §9.3（本文書へ移設）  |

### フェーズ2: consult SKILL.md の置き換え対応表

現行の Phase 2（討議ファイルの用意）・Phase 4（進行における討議ファイル更新）・Phase 5（終了時のファイル参照）を、`agenda_store.py` / `agenda_render.py` の CLI 呼び出しに置換する。

| 現行（Markdown 自前実装）                                         | 変更後（agenda 機構）                                                                                                                                                                         |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.claude/.temp/consult/<日付>-<主題>.md` を Write で新規作成      | `agenda_store.py init` を呼ぶ                                                                                                                                                                 |
| 討議ファイルへの Edit（決着・状態更新）                           | `agenda_store.py update --item-file <item.json>` を呼ぶ                                                                                                                                       |
| 討議ファイルを Read して既存ファイル・再開候補を確認（Phase 2.1） | `agenda_store.py pending` / `next` を呼ぶ                                                                                                                                                     |
| コンソールへのアジェンダ表再掲（Phase 4 手順 6）                  | `agenda_render.py` を明示的に呼ばない（`update` 完了時に自動生成済み。[DES-075](../design/DES-075_agenda_mechanism_design.md) §8.3）。生成済みの `agenda.html` のパスを利用者へ示すだけでよい |

### フェーズ3: discussion_file_template.md の転用

`plugins/forge/skills/consult/assets/discussion_file_template.md` は、保存フォーマットとしての役目を終え、`agenda_render.py` が生成する HTML の構造テンプレート（`plugins/forge/scripts/agenda/templates/agenda_display_template.html` 等）としてリライトする。「書くときの約束」節（ID 不変・状態行を消さない等）は `agenda_store.py` の状態遷移ルール（[DES-075](../design/DES-075_agenda_mechanism_design.md) §5.1）が機械的に強制するため、散文としての役目は縮小する。

### フェーズ4: 既存記録の破棄（FNC-010）

- `.claude/.temp/review/triage.md`（存在する場合）・`.claude/.temp/consult/*.md` を破棄する
- 破棄前に、利用者が失う内容（未決着の議題があれば）を一覧して伝える
- 破棄後に放置される記録が残らないことを実装時に確認する

## 検証ポイント

| フェーズ | 完了時の検証事項                                                                                                                                                                   |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | `agenda_store.py init → update → record-structural-judgment → next/pending` が単体で意図通り動作する。`agenda_render.py` が単体で正しい HTML を生成する                            |
| 2        | 既存 `consult` の Phase 0〜5 の対話フローが、置き換え後も同じ利用者体験を提供する（討議ファイルの代わりに `agenda.json`/`agenda.html` を使うだけで、対話進行そのものは変わらない） |
| 3        | リライトしたテンプレートで生成した HTML が、既存の討議ファイルが持っていた情報（アジェンダ表・項目ごとの背景/本質/推奨/決着）を過不足なく表示できる                                |
| 4        | 既存記録の破棄後、`.claude/.temp/review/` `.claude/.temp/consult/` に放置ファイルが残っていない                                                                                    |

## リスクと対策

| リスク                                                            | 対策                                                                                                                                             |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| フェーズ4の既存記録破棄で、未決着の議題を利用者に気づかせずに失う | フェーズ4実行前に、破棄対象ファイルの内容（未決着項目の有無）を一覧して利用者に確認を取ってから破棄する（FNC-010 が要求する事前把握）            |
| フェーズ2の書き換え中、consult の対話フロー自体を壊す             | フェーズ1で新機構を単体動作確認してから着手することで、フェーズ2で問題が起きた場合に「新機構のバグか、置き換え作業のミスか」を切り分けやすくする |

## 既存資産の活用

再利用する既存コンポーネントの一覧は [DES-075](../design/DES-075_agenda_mechanism_design.md) §11「使用する既存コンポーネント」を参照する（本文書では転記しない）。
