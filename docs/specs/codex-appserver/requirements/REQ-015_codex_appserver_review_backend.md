# REQ-015 codex-appserver レビューバックエンド要件定義書

## メタデータ

| 項目         | 値                                                                                                |
| ------------ | ------------------------------------------------------------------------------------------------- |
| 要件 ID      | REQ-015                                                                                           |
| サブシステム | codex-appserver（Codex app-server を実行主体とするレビューバックエンド）                          |
| 種別         | 要件定義（新規バックエンド・実験段階）                                                            |
| 対象         | `/forge:review` のバックエンド軸に接続し、Codex app-server 上の自前スレッドでレビューを実行する層 |
| 関連要件     | forge:REQ-013 FNC-1318（バックエンド軸と入出力契約）                                              |
| 関連設計     | ADR-063（本バックエンドを立てる決定）、DES-064（設計）                                            |
| 作成日       | 2026-08-02                                                                                        |

> **実装ブロック中（2026-08-02）[MANDATORY]**
>
> 本バックエンドは **forge:REQ-013 FNC-1318 の改訂を待って実装に着手する**。現行契約には失敗・レビュー終了・位置未確定を表す手段が無く、これらを契約外の暫定挙動で代替すると FNC-001「契約を満たすこと」に反する。必要な改訂は §5 TBD-005〜007 の 3 点。
>
> 本文書と DES-064 は、その改訂を前提とする仕様として維持する。**契約外の暫定挙動は定めない。**

> **本文書のスコープ**
>
> - **定めること**: レビュー実行主体として Codex app-server を用いる際の要件。スレッドの所有、応答の共通書式への変換、往復の監査、前提検査、失敗の確定
> - **定めないこと**: レビュー依頼の組み立て・所見の評価と修正・対象解決・観点の名指し・完了判定。これらは forge:REQ-013 FNC-1318 により `/forge:review` 本体の責務である
> - **他バックエンドとの関係**: msg-review バックエンドを置き換えない。両者は `--backend` で選択される並列の手段であり、**等価ではない**（人間の途中介入は msg-review のみが提供する）。本文書は msg-review / msg-sys の仕様を変更しない

---

## 1. 背景

### 1.1 現状の問題

forge:ADR-060 / REQ-013 FNC-1318 がレビュー実行主体を `--backend` で交換可能にしたが、現時点で存在するバックエンドは msg-review のみである。msg-review は「人間が常駐させた Codex TUI セッション・Stop フックによる pull 型の新着検知・相手のターンを起こす起床手段」という前提の上に成り立つ。

この前提を満たせないプロジェクトでは、実行主体だけでなく**レビューフロー全体**が使えない。とくに起床手段は端末多重化ツール（cmux）に依存しており、これを使わない環境では往復が沈黙したまま停止する（msg-review:DES-045 に、約 7 時間の待機・往復 2 ラウンド目以降の停止・1 レビューで 4 回の手動起床という実測が記録されている）。

### 1.2 本バックエンドの位置づけ

Codex CLI は app-server サブコマンドを持ち、JSON-RPC でスレッドの開始・再開とターンの実行を制御できる。**バックエンド自身がスレッドを所有する**構成を採れば、相手は人間の TUI ではなくバックエンドが管理するスレッドになる。

この構成では起床という概念が存在しない。ターンを開始するのはバックエンド自身だからである。したがって端末多重化ツール・起動シム・Stop フック・メッセージ DB のいずれも不要になる。

単発起動（`codex exec`）との違いはスレッドを保持することであり、複数ラウンドにわたって文脈が継続する。

---

## 2. スコープ

### 2.1 対象とする範囲（v1）

- forge:REQ-013 FNC-1318 の入出力契約を満たすレビューバックエンドとして動作すること
- 自前のスレッドを保持し、同一レビュー内の複数ラウンドで文脈を継続すること
- Codex の構造化応答を、全バックエンド共通の所見書式へ変換すること
- 往復の内容を監査可能な形で記録すること
- 前提が成立しない場合に、依頼を送る前に検出して報告すること

### 2.2 v1 で明示的に除外する範囲

- **レビュー依頼の組み立てと所見の処理**: 対象解決・観点の名指し・粒度保存・所見の評価と修正・完了判定は `/forge:review` 本体の責務である（forge:REQ-013 FNC-1318）
- **人間の途中介入と進行の可視化**: レビューの進行を画面で見て途中で介入する用途は本バックエンドでは扱わない。この性質が必要な場合は msg-review バックエンドを選ぶ
- **msg-review / msg-sys への変更**: 本バックエンドは両者の要件・設計・実装のいずれも変更しない。起床・Stop フック・メッセージ DB を使わない
- **Codex 以外のレビュアー**: app-server は Codex 固有のプロトコルである。他のレビュアーを使う場合は別バックエンドとする

### 2.3 v1 の位置づけ（意図的な判断）

v1 は、バックエンド軸に 2 つ目の実装を接続することで **forge:ADR-060 が「1 実装しかない期間は契約が実際に非依存かどうか検証できない」と述べた点を実地に検証する**位置づけを兼ねる。契約に不足が見つかった場合は forge:REQ-013 側の改訂として扱い、本文書で回避策を持たない。

---

## 3. 機能要件

### FNC-001: バックエンド契約の充足 [MANDATORY]

forge:REQ-013 FNC-1318 が定める入出力契約を満たすこと。

- 入力として `review_id`、ラウンド番号、依頼本文（テンプレート展開済み）を受け取ること
- 出力として判定（`approved` / `findings`）と所見配列（重大度・ファイルパス・行・本文）を返すこと
- **バックエンド固有の事情を本体へ漏らさないこと**。app-server の接続方式・スレッド管理・プロトコルの詳細を本体の関心事にしない

**本要件は現行契約のもとでは充足できない（実装ブロック）[MANDATORY]**: 後述 FNC-002 / FNC-003 / FNC-005 が必要とする 3 つの表現——失敗・レビュー終了・位置未確定——のいずれも現行契約に存在しない。これらを契約外の暫定挙動で代替すると、本要件が求める「契約を満たす」状態にならない。契約にない結果（呼び出しエラー）を返す、必須項目（ファイルパス・行）を欠いた所見を返す、終了通知を得ないまま資源を推測で操作する、はいずれも充足ではなく逸脱である。

したがって本バックエンドは、**forge:REQ-013 FNC-1318 が §5 TBD-005〜007 の 3 点について改訂されるまで実装に着手しない**。本文書と DES-064 は、その改訂を前提とする仕様として維持する。契約外の暫定挙動は定めない。

### FNC-002: スレッドの所有と文脈継続 [MANDATORY]

本バックエンドがレビュー用のスレッドを所有し、同一 `review_id` の複数ラウンドで文脈を継続すること。

- 人間が起動した TUI セッションに接続しないこと。相乗りは利用者の起動経路への介入を要するため採らない
- ラウンド間でスレッドを保持し、前ラウンドの指摘と修正の経緯を Codex 側が参照できること
- **保持した資源（app-server プロセス・スレッド・記録）を最終的に必ず解放すること**

**現行契約に終了の通知が無い（契約ギャップ）**: forge:REQ-013 FNC-1318 の入力はラウンドごとの `review_id`・ラウンド番号・依頼本文のみであり、**レビューが終わったことをバックエンドへ伝える経路が無い**。レビューは `approved` 以外でも終わる（本体の介入軸による打ち切り、失敗の確定、利用者の中断）ため、`approved` を返したことを終了の代替信号として扱っても解放漏れが残る。次ラウンドを待ち続けて app-server と記録がリークする。

**時間経過による回収では代替できない**。一定時間使われていない記録を次回起動時に走査する方式を検討したが、次の 2 点で「最終的に必ず解放する」を満たさない。

- 次回の起動が無ければ走査自体が行われない。最後のレビューの資源は残り続ける
- 人間の介入で長時間中断しているレビューを、経過時間だけを根拠に破棄しうる。継続中の資源を奪う

したがって本要件は、**明示的なライフサイクル操作（finalize / cancel 相当）、または本体側に完了後のクリーンアップ呼び出しを義務づける契約**を前提とする。これが forge:REQ-013 側で合意・改訂されるまで、本要件は充足できない（FNC-001 の実装ブロック。§5 TBD-006）。

### FNC-003: 共通書式への変換 [MANDATORY]

Codex から返る構造化された応答を、全バックエンド共通の所見書式（自由記述 markdown + 重大度マーカー + 完了宣言行）へ変換すること。

forge:ADR-060 は書式契約を全バックエンド共通に固定し、**構造化された形式で応答するレビュアーを使う場合の変換はバックエンドの内部責務**と定めている。本バックエンドはこれに該当する。

- 変換の失敗（期待する重大度マーカーや完了宣言行を取り出せない）を、所見なし（`approved`）と誤って扱わないこと
- 依頼テンプレートをバックエンド固有に分岐させないこと

**共通書式に位置情報の機械可読な形式が無い（契約ギャップ）**: 共通の所見書式は「自由記述 markdown + 重大度マーカー + 完了宣言行」であり、**ファイルパスと行番号の表現形式を規定していない**。一方 forge:REQ-013 FNC-1318 の出力は所見ごとに重大度・ファイルパス・行・本文を要求する。両者の間に落差があり、位置が書かれていない所見・曖昧な所見・複数箇所にまたがる所見をどう扱うかが未定義である。

この落差は観測済みである。本バックエンドの仕様策定にあたり実施したレビューで、レビュアーは所見に `パス:行` を明記したにもかかわらず、共通書式のパーサは 3 件すべてで位置を抽出できなかった。

**契約外の表現で代替しない**。位置を欠いたまま所見を返すことは、必須項目を満たさない出力を返すことであり充足ではない。位置を推測で埋めることは、存在しない指摘箇所を本体に信じさせる。所見を破棄することは、指摘そのものを消す。いずれも採らない。

したがって本要件は、**共通書式への位置情報の必須形式の追加、または位置未確定を表現できる所見スキーマの定義**を前提とする。これが forge:REQ-013 側で合意・改訂されるまで、本要件は充足できない（FNC-001 の実装ブロック。§5 TBD-007）。

### FNC-004: 前提条件の事前検査 [MANDATORY]

依頼を送る前に前提を検査し、不成立の場合は依頼を送らず利用者に不足を報告すること。少なくとも以下を検査する。

- Codex CLI が利用可能で、app-server を提供すること
- 本バックエンドが用いるプロトコルのメソッドが、その Codex に存在すること
- app-server を起動または再利用できること

### FNC-005: 失敗の確定と報告 [MANDATORY]

前提検査の通過後に失敗した場合、結果を保留せず、そのラウンドの失敗として確定して本体へ返すこと。

- 応答を得られないまま無期限に待ち続けないこと
- 失敗を所見なし（`approved`）へ畳み込まないこと
- 別バックエンドへ暗黙にフォールバックしないこと（forge:REQ-013 FNC-1318 の fail closed と同旨。利用者が意図しない実行主体でレビューが走ることを避ける）

**現行契約に failure の表現が無い（契約ギャップ）**: forge:REQ-013 FNC-1318 のバックエンド出力は判定（`approved` / `findings`）と所見配列のみであり、**失敗を表す値を持たない**。したがって本要件が求める「失敗として確定して返す」を契約の戻り値では表現できない。本バックエンドは暫定として次のとおり振る舞う。

**判定値で代替しない**。失敗時に `approved` を返せば所見なしとして完了し、所見 0 件の `findings` を返せば矛盾した状態になる。どちらも本体は正常な判定として扱うため、失敗が消える。

**呼び出しエラーでも代替しない**。非ゼロ終了は契約の戻り値に含まれない結果であり、本体がそれをどう扱うかも定義されていない。契約外の経路で失敗を伝えることは充足ではない。

したがって本要件は、**契約が failure を判別可能な結果として表現できること**を前提とする。これが forge:REQ-013 側で合意・改訂されるまで、本要件は充足できない（FNC-001 の実装ブロック。§5 TBD-005）。

### FNC-006: 実行権限の限定 [MANDATORY]

レビューは対象を読んで所見を述べる作業であり、対象の変更を伴わない。本バックエンドが開始するターンには、レビューに必要な範囲を超える権限を与えないこと。

- 具体的な権限設定の値は設計で確定する（§4 TBD-002）

### BL-001: 往復の監査可能性

レビューの往復内容（各ラウンドの依頼本文と応答）を、後から参照できる形で記録すること。

msg-review はこれを msg-sys の履歴に委ねているが（msg-review:REQ-012 BL-001）、本バックエンドはメッセージ DB を使わないため自前で満たす必要がある。

- 記録は `review_id` 単位で辿れること
- 複数のレビューが時間的に離れて行われた場合でも、対象レビューの往復のみを扱い、他のレビューと混同しないこと

---

## 4. 非機能要件

### NFR-001: 標準ライブラリのみでの実装 [MANDATORY]

Python 実装は標準ライブラリのみを使用し、外部パッケージへの依存を持たない。Bash からの外部コマンド呼び出し（`codex` 等）は許容する。

app-server との通信に WebSocket が必要であるが、これも標準ライブラリの範囲で実装する（同梱の中継サブコマンドはハンドシェイクを代行せず、素の JSON-RPC も受け付けないことを実測で確認済み。msg-review:ADR-061 §1）。

### NFR-002: 外部ツール非依存 [MANDATORY]

エージェント間メッセージング基盤等の外部プラグイン・パッケージを実行時依存として導入しない。端末多重化ツールにも依存しない。

### NFR-003: 位置づけと適用範囲

本バックエンドは本番運用・外部公開を前提としない実験段階の内部ツールである。性能目標値は定めない。恒久化の要否は、バックエンド軸に 2 つ目の実装が接続された状態での運用結果を踏まえて判断する（§2.3）。

---

## 5. 未確定事項

| ID      | 内容                                                                            | 解決方法                                                                                                  |
| ------- | ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| TBD-001 | app-server 上の自前スレッドでレビュー 1 往復が成立すること                      | 実機確認（ADR-063 を accepted へ移す条件）                                                                |
| TBD-002 | FNC-006 の権限設定の具体値（サンドボックスと承認ポリシー）                      | 設計で確定する                                                                                            |
| TBD-003 | ターンの開始に用いるメソッドの選択                                              | 実機確認のうえ設計で確定する                                                                              |
| TBD-004 | 本バックエンドを接続した結果、forge:REQ-013 FNC-1318 の契約に不足が出るか       | **不足が 3 件顕在化した（TBD-005〜007）。§2.3 の想定どおり、いずれも forge 側の改訂として提起する**       |
| TBD-005 | 契約に failure を表す値が無く、失敗を戻り値で表現できない（FNC-005）            | forge:REQ-013 FNC-1318 の改訂として、判別可能な結果型または呼び出しエラーの契約化を提起する               |
| TBD-006 | 契約にレビュー終了を伝える経路が無く、資源の解放契機を得られない（FNC-002）     | forge:REQ-013 FNC-1318 の改訂として、明示的なライフサイクル操作か本体側クリーンアップ義務の追加を提起する |
| TBD-007 | 共通書式に位置情報の機械可読な形式が無く、出力の必須項目を満たせない（FNC-003） | forge:REQ-013 FNC-1318 の改訂として、位置の必須形式または位置未確定を表せるスキーマの定義を提起する       |
